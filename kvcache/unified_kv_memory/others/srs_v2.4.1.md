# KVCache SRS 需求列表 v2.4

## 横向模块分类

| 顶层业务模块 (Top-Level Module) | 模块核心职责描述 | L1 推理调度层需求 ID | L2 KVConnector 层需求 ID | L3 传输管理层需求 ID | L4 底层传输层需求 ID |
| --- | --- | --- | --- | --- | --- |
| TM1: 推理调度与标准接口控制 | 负责北向意图解析、软硬件协同路由决策、级联水位强反压准入与框架协议标准化。<br>1.1 北向意图解析<br>1.2 软硬件协同路由决策<br>1.3 级联水位强反压准入<br>1.4 框架协议标准化 | L1-PM-PrefixBudget-001, L1-RT-Admission-007, L1-RT-CacheAwareRouting-006, L1-VLLM-HIER-SCHED-002, L1-SE-IntentAPI-014, L1-SE-ExpertOverride-015, L1-RT-WatermarkAdmission-024 | L2-CONN-API-STD-001, L2-CONN-TOPO-ROUTE-002, L2-CONN-WatermarkQuery-032, L2-KV-PublishCommit-033, L2-KV-SemanticIdentity-036, L2-CONN-CostAwareReturn-039, L2-CONN-BufferContract-040 | L3-SE-PolicyEngine-032, L3-SE-UnifiedDescriptor-018, L3-TRANS-CAP-API-002, L3-TRANS-SEM-ENGINE-001, L3-SE-GranularityDispatch-033, L3-SE-ViewCopyCostModel-034, L3-TRANS-TOPO-SENSE-004, L3-SE-PlacementResolver-061, L3-SE-QueryPlanFastPath-072, L3-SE-DescriptorFromManifest-079, L3-SE-MultiReplicaResolver-089 | L4-FABRIC-ROUTER-001, L4-HW-SemCapTable-050, L4-HW-AddrTrans-051, L4-HW-StorageLayoutCapability-067, L4-HW-NICSpecConstraint-075 |
| TM2: 分布式前缀索引与元数据平面 | 负责微妙级前缀安全碰撞判定、多级目录镜像同步与全局一致性哈希路由。<br>2.1 微秒级前缀安全碰撞判定<br>2.2 多级目录镜像同步<br>2.3 全局一致性哈希路由 | L1-SGL-PFX-IDX-003, L1-VLLM-PFX-SCHED-006, L1-VLLM-PFX-IDX-005 | L3-CONN-PFX-IDX-005, L2-CONN-META-CACHE-004, L2-PM-BatchLookup-021, L2-KV-ConsumeEligibility-035 | L3-MS-DirectoryMirror-036, L3-MS-MetadataDataPlane-035, L3-MC-PFX-IDX-003, L3-MC-PFX-REPL-004, L3-MS-MultiReplicaDirectory-037, L3-PM-HitQuality-023, L3-MS-ReplicaPlacementState-059, L3-MS-ConsumeEligibility-060, L3-MS-InvalidationTombstone-066, L3-MS-StaleHitGuard-067, L3-MS-PrefixDirectorySchema-069, L3-MS-HotLocalIndex-070, L3-MS-RangeBatchLookup-071, L3-MS-DirectoryShardPolicy-073, L3-MS-MetadataCompactLayout-074, L3-MS-AtomicPublishVisibility-087, L3-MS-TTFTIndexLayout-088 | - |
| TM3: 异构分层存储池与生命周期空间 | 负责精细化容量预估、冷热状态机流转、异步内存紧凑整理以及基于多级水位的智能主动搬移决策。<br>3.1 精细化容量预估<br>3.2 冷热状态机流转<br>3.3 异步内存紧凑整理<br>3.4 基于多级水位的智能主动搬移决策 | L3-MM-Lifecycle-009, L1-MM-KVSizing-008, L1-OL-ActiveWarmClass-010, L1-OL-PartialBoundary-012, L1-OL-ViewVsCopy-011, L1-MM-DefragAware-023 | - | L3-MC-POOL-GLOBAL-001, L3-MS-Tiering-038, L3-MC-HIER-STORE-002, L3-MS-CostEviction-039, L3-MC-POOL-ALLOC-005, L3-MC-UBAllocator-049, L3-MC-CompactionEngine-056, L3-MC-IntelligentMigration-057, L3-MS-KVObjectStateMachine-058, L3-MS-MigrationInterlock-062, L3-MC-KVBlockStorageSchema-075, L3-MC-ExtentManifest-076, L3-MC-PageExtentAllocator-077, L3-MC-LayoutTransformPlan-078, L3-MS-StateAwareEviction-082 | L4-C2C-UNIFY-POOL-001, L4-UB-C2C-UNIFY-002, L4-UB-P2P-FABRIC-003, L4-UB-DirectViewGuard-072 |
| TM4: 硬件加速传输与数据流编排 | 负责多层级流水线重叠、投机预取、散射收束描述符转换及底层零拷贝、DPU 硬件写回加速。<br>4.1 多层级流水线重叠<br>4.2 投机预取与散射收束描述符转换<br>4.3 底层零拷贝与 DPU 硬件写回加速 | L1-SGL-PRFCH-SCHED-004, L1-SGL-SWP-XFER-002, L1-VLLM-PRFCH-SCHED-004, L2-CONN-SWP-XFER-003 | L2-CONN-PRFCH-ENGINE-007, L2-OL-BulkDescriptor-025, L2-OL-LayoutNegotiation-024, L2-KV-PartialAttachPlan-038 | L3-MS-HotReplication-040, L3-TRANS-MUL-ENGINE-003, L3-C2C-POOL-SCHED-002, L3-LMC-PRFCH-ENGINE-001, L3-UB-TOPO-ROUTE-001, L3-MS-StateAwarePrefetch-081 | L4-OL-RegisteredPool-053, L4-RDMA-P2P-NPU-001, L4-RDMA-MUL-FABRIC-002, L4-MC-HIER-STORE-001, L4-MC-HIER-STORE-002, L4-MC-HIER-STORE-003, L4-NET-OFFLOAD-DPU-001, L4-MC-PersistentExtentHandle-070, L4-RDMA-RemoteExtentHandle-071 |
| TM5: 共享协同、安全隔离与 QoS 管控 | 负责多租户安全隔离、多卡共识、租约级内存无感原子迁移以及主动搬移流量的硬件级隔离。<br>5.1 多租户安全隔离与多卡共识<br>5.2 租约级无感原子迁移<br>5.3 搬移流量的硬件级 QoS 隔离 | L1-SC-TenantIsolation-017, L1-PD-RankConsensus-013 | L2-MM-ViewLease-028, L2-MM-MultiConsumer-027, L2-CONN-DefragPause-031, L2-KV-AttachHandle-034 | L3-CO-VersionPublish-043, L3-QO-SemanticQoS-045, L3-SC-PoolIsolation-048, L3-CO-AttachDetachLease-063, L3-CO-VisibilityReadyBitmap-064, L3-CO-PublishCommitLog-065, L3-CO-ReplicaQuarantine-068, L3-CO-KVOwnershipGC-080, L3-CO-RefCountLifecycle-086, L3-CO-MigrationRCULock-090 | L4-CO-FencePrimitive-052, L4-QO-TrafficClass-056, L4-SC-ViewProtection-062, L4-CO-PageMigration-063, L4-QO-MigrationQoS-064, L4-CO-AtomicRemapPrimitive-065, L4-CO-ExtentVisibilityFence-066, L4-QO-StateAwareTrafficClass-068, L4-CO-DrainAndQuiesce-073, L4-SC-SecureExtentRelease-074 |
| TM6: 全路径全栈可观测性与容错保障 | 负责全链路性能观测追踪（命中了为什么变慢）、路径降级因果追溯与底层 RAS 错误向标准上层错误码的映射。<br>6.1 全链路性能观测追踪<br>6.2 路径降级因果追溯<br>6.3 底层 RAS 错误标准映射 | L1-OB-SemanticMetrics-016 | L2-FT-FallbackContract-029, L2-OB-PathTrace-030, L2-KV-StateErrorCode-037 | L3-OB-PerPathTelemetry-047, L3-FT-FallbackTrace-046, L3-MS-UBC2CTier-055, L3-OB-KVStateTrace-083, L3-CTRL-KVInspectAPI-084 | L4-FT-RASErrorMap-061, L4-OB-RemoteAccessCounter-057, L4-FT-ReplicaIntegrityCheck-069 |

## L1 推理调度层

| 需求唯一标识 | 优先级 | 核心模块 | 需求描述 | 重点针对场景 |
| --- | --- | --- | --- | --- |
| L1-PM-PrefixBudget-001 | P0 | 调度与路由控制 | 本需求要求在 Scheduler 中实现前缀判断预算控制机制：每个请求在进入 prefix lookup 前生成 prefix_decision_deadline_ns 与 max_remote_lookup_us，所有本地索引、远端目录查询、load/recompute 判断必须在预算内完成；若超过预算，系统必须立即降级为 partial hit 或 recompute，避免 raw hit 转化为 TTFT 负收益。 | 共享前缀命中快速判定 |
| L1-RT-Admission-007 | P0 | 调度与路由控制 | 本需求要求 Scheduler 在 prefix hit 后执行 load-vs-recompute admission 判断：只有当 lookup + transfer + attach + rank_sync 小于 recompute_saved_time 且满足 TTFT deadline 时才允许加载 KV，否则必须直接 recompute 或 partial recompute。 | 命中 KV 加载收益判定 |
| L1-RT-CacheAwareRouting-006 | P1 | 调度与路由控制 | 本需求要求 Router 在选择推理实例前查询 KV placement summary，优先将请求路由到已拥有对应 prefix KV 的 HBM、本地 DDR 或同机架远端 DDR 节点；在相同负载下，应优先提升 local usable hit rate，而不是先调度后远端搬运 KV。 | KVCache 亲和路由调度 |
| L1-VLLM-HIER-SCHED-002 | P1 | 调度与路由控制 | 本需求要求扩展vLLM BlockSpaceManager，支持GPU HBM/本地DDR/远端DDR/SSD四层存储状态的枚举、查询与调度决策，使调度器能感知并利用集群级分布式KVCache中的数据，在"加载远端KVCache"与"重新计算"之间做出最优决策，避免不必要的GPU重计算。 | 多层异构存储调度 |
| L1-SGL-PFX-IDX-003 | P1 | 前缀索引与命中判定 | 本需求要求将SGLang RadixTree的最长前缀匹配（LPM）核心遍历函数从Python实现迁移至C++/Cython，通过SIMD（AVX2）加速节点键的字符串比较操作，并引入RCU无锁并发设计允许多个LPM查询同时执行，同时采用路径压缩减少RadixTree节点数量。 | 前缀匹配关键路径加速 |
| L1-VLLM-PFX-SCHED-006 | P1 | 前缀索引与命中判定 | 本需求要求将前缀匹配检测（哈希计算+本地查询+远端状态确认）从Scheduler决策的同步串行路径中解耦，通过独立前缀预检测Worker Pool异步执行，使Scheduler在决策时能直接读取已完成结果，远端RTT不再出现在调度关键路径上。 | 前缀匹配关键路径解耦 |
| L1-VLLM-PFX-IDX-005 | P2 | 前缀索引与命中判定 | 本需求要求将vLLM的前缀哈希计算从CPU Python层迁移至GPU端并行计算（xxHash-128  Kernel），引入每25tokens的分层哈希Checkpoint实现增量计算，并将哈希计算与token embedding并行化执行，将前缀索引计算耗时从10-20ms压缩至0.5ms以内，消除前缀检测对TTFT的显著贡献。 | 前缀匹配关键路径 |
| L1-SGL-PRFCH-SCHED-004 | P2 | 卸载/加载与预取优化 | 本需求要求在SGLang中实现基于请求到达批次分析的投机性前缀预取机制，通过检测短时间窗口内同前缀请求的批量到达模式，主动触发OFFLOADED状态RadixNode从CPU DDR向GPU HBM的异步预取，将KVCache加载时延隐藏在请求排队等待阶段。 | 投机性前缀预取 |
| L1-SGL-SWP-XFER-002 | P2 | 卸载/加载与预取优化 | 本需求要求实现SGLang的Layer级流式KVCache加载Pipeline，将整体KVCache加载从"等待全部L层就绪才开始计算"改为"每K层就绪即触发对应层的Attention计算"，通过分级 Stream与Event同步实现KVCache加载与prefill计算的深度流水线重叠。 | 流式KV加载Pipeline |
| L1-VLLM-PRFCH-SCHED-004 | P2 | 卸载/加载与预取优化 | 本需求要求在vLLM Scheduler中实现N步Lookahead预测机制，对请求队列中即将被调度的序列提前发起低优先级异步KVCache预取，使数据传输时延与请求排队等待时延充分重叠，从而消除Swap In等待对TTFT关键路径的影响。 | 请求调度预取优化 |
| L1-MM-KVSizing-008 | P1 | 容量估算与生命周期 | 本需求要求提供模型结构感知的 KV block/page 容量估算 API，覆盖 MHA、GQA、MLA、sliding-window、hybrid/recurrent state、TP rank、dtype、layout version 等因素，确保 HBM/DDR/CXL/SSD 预留容量与实际 KV footprint 误差小于 5%。 | 模型 KVCache 容量规格化 |
| L1-OL-ActiveWarmClass-010 | P1 | 驻留策略与局部复用 | 本需求要求框架层显式区分 decode-active KV 与 warm prefix KV：decode-active KV 默认必须驻留 HBM，warm prefix KV 可驻留本地 DDR、本地DDR、本地SSD 或远端节点；系统不得把低带宽 memory tier 直接当作 HBM 扩容使用。 | 活跃 KV 与热 KV 分层驻留 |
| L1-OL-PartialBoundary-012 | P2 | 驻留策略与局部复用 | 本需求要求在 partial prefix hit 场景中自动选择最优 recompute boundary：系统应根据 block/page 边界、命中长度、load 时间和 recompute 时间决定 “加载已命中 prefix + recompute suffix” 的切点，而不是在 partial hit 时简单放弃全部命中。 | 部分前缀命中复用 |
| L1-OL-ViewVsCopy-011 | P2 | 驻留策略与局部复用 | 本需求要求实现 direct-view eligibility 判断：当 KV 对象为 metadata、短 prefix span 或 warm preview 时允许memory view；当 KV 将进入 decode attention active path 时，默认 copy-to-HBM，除非 cost model 证明 direct view 不影响 TPOT。 | 热 KV 近端访问与 HBM 回填选择 |
| L1-SC-TenantIsolation-017 | P0 |   | 本需求要求 KV key 构造必须包含 tenant、security domain、cache salt、model id、tokenizer hash、template version、layout version 等隔离字段；公共 KV 必须显式标记 shareable，私有 KV 不得跨租户命中或共享。 | 多租户 KVCache 隔离共享 |
| L1-SE-IntentAPI-014 | P0 | 多卡协同与观测治理 | 本需求要求北向接口默认采用 KVAccessIntent 语义：上层只描述操作目的、deadline、KV 大小、reuse value、visibility、fallback policy、isolation domain，不直接绑定 UB.memory、RDMA、TCP 等具体路径，由下层 policy engine 决定执行语义。 | 统一 KVCache 访问意图编排 |
| L1-OB-SemanticMetrics-016 | P1 | 多卡协同与观测治理 | 本需求要求框架层输出语义化 KV 命中指标，不得只统计 prefix hit rate；必须区分 raw hit、usable hit、local usable hit、view hit、bulk-load hit、stream-restore hit、abandoned hit，并记录每次请求 prefix critical path breakdown。 | KVCache 命中收益可观测 |
| L1-PD-RankConsensus-013 | P1 | 多卡协同与观测治理 | 本需求要求在 TP/PP/P-D 分离场景中实现 rank-level prefix consensus：所有参与 rank 必须对 usable prefix length、KV version、ready bitmap、layout version 达成一致后才允许 attach；若 rank 间不一致，必须取 min-safe prefix 或降级 recompute。 | 多卡共享前缀一致复用 |
| L1-SE-ExpertOverride-015 | P3 | 多卡协同与观测治理 | 本需求要求在默认 intent API 之外提供专家级 override：允许研发和运维在 A/B 实验、故障绕行、极致性能调优中显式指定 preferred semantics、forbidden semantics、force copy-to-HBM、allow direct view、forbid TCP fallback 等策略。 | 专家级 KV 传输策略控制 |
| L1-MM-DefragAware-023 | P1 | 容量估算与生命周期 | 本需求要求 L调度器支持内存紧凑（Compaction）感知：当底层触发碎片整理时，调度器需动态规避正在被迁移的物理 Block，或通过热备 Block 替换机制维持当前推理序列的连续写入，避免碎片整理导致推理线程死锁或停顿。 | 内存高碎片率推理不中断 |
| L1-RT-WatermarkAdmission-024 | P0 | 调度与路由控制 | 本需求要求调度器引入水位驱动的强反压准入策略。当接收到 L的 Critical 水位告警时，立即切断低优先级请求的 KV 加载意图，强制将新请求降级为 Recompute 或执行排队减速，保护系统不发生 HBM OOM。 | 级联水位高压反压准入 |

## L2 KVConnector层

| 需求唯一标识 | 优先级 | 核心模块 | 需求描述 | 重点针对场景 | 备注 |
| --- | --- | --- | --- | --- | --- |
| L2-CONN-API-STD-001 | P0 | 统一协议与标准接口 | 本需求要求定义并实施统一的KVConnector Protocol标准接口（Python Protocol + gRPC IDL双形式），覆盖put/get/prefetch/evict/get_status/get_transport_stats六类操作，制定KVPage/{ptr, mem_type, size, layer_idx}和KVMeta/{model_id, prefix_hash, dtype, kv_shape}标准数据结构，并制作conformance test suite，使vLLM和SGLang共用同一套Connector规范。 | 统一接口标准化 |  |
| L2-CONN-TOPO-ROUTE-002 | P1 | 路径路由与能力探测 | 本需求要求在Connector层实现传输路径质量动态监控与自适应路由能力，通过100ms周期的后台探测实时感知各传输路径的BW和时延，基于传输大小/SLO/路径质量综合评分自动选择最优传输后端，并支持路径故障时200ms内自动完成切换。 | 动态传输路径路由 |  |
| L2-CONN-META-CACHE-004 | P1 | 本地元数据缓存与快速判定 | 本需求要求在Connector层维护本地in-process元数据缓存（HashMap，默认100K条目），缓存prefix_hash到存储位置的映射关系，通过TTL过期和L3层版本号失效通知保证缓存一致性，使高频复用场景下的元数据查询从5-20ms RTT降至10μs以内。 | 前缀命中快速决策 |  |
| L2-PM-BatchLookup-021 | P1 | 本地元数据缓存与快速判定 | 本需求要求 Connector 提供 batch prefix lookup API：一次提交 block hash vector、radix span 或 page hash range，返回连续命中区间、tier、location、version 和 estimated load cost，避免每个 block/page 单独访问远端目录造成 metadata RTT 风暴。 | 共享前缀 KVCache 批量查询 |  |
| L2-CONN-PRFCH-ENGINE-007 | P2 | 预取触发与命中评估 | 本需求要求在Request Arrival阶段实现主动KVCache预取触发机制，当新请求到达Waiting队列时立即查询前缀存储状态（利用L2本地缓存，μs级），对REMOTE_DDR状态的KVCache发起非阻塞式prefetch，并预分配目标NPU Buffer，使KVCache加载时延在请求排队期间完成，预取Buffer利用率目标≥80%。 | 前缀KV隐藏加载时延 |  |
| L2-OL-BulkDescriptor-025 | P1 | 传输描述与布局协商 | 本需求要求 Connector 将多个不连续 KV block/page 合并为 scatter-gather bulk descriptor，支持 coalescing、max descriptor count、min transfer granularity、batch submit，避免 for-loop 式小块传输放大 CPU submit 与 DMA/RDMA setup 开销。 | KVCache 批量卸载与批量加载 | 当前就有batch接口，挪到L3/L4。<br>1. 底层是否有并发传输能力；<br>2. 并发传输的stream流是否有数量限制。 |
| L2-OL-LayoutNegotiation-024 | P1 | 传输描述与布局协商 | 本需求要求 Connector 与后端协商 KV layout：对 RDMA优先选择 MB 级连续 cross-layer block，对 SGLang HiCache/L存储支持 page-first/page-first-direct，对 SSD/object 支持 segment layout，减少 relayout 和小 descriptor 传输。 | KVCache 高效传输布局协商 | 1. 前缀索引表的数据结构<br>2. Kvcache的存储块结构<br>3. 从查询前缀到取回KV全流程加速设计<br>4. 底层传输多路径的路径拥塞和优化 |
| L2-FT-FallbackContract-029 | P0 | 共享访问与故障控制 | 本需求要求 Connector 严格执行 fallback contract：若上层 intent 禁止 TCP、SSD 或 object fallback，则 Connector 不得内部静默降级；若 deadline 到期仍未 ready，必须返回 partial hit、miss 或 recompute signal。 | KV 加载失败快速降级恢复 |  |
| L2-MM-ViewLease-028 | P0 | 共享访问与故障控制 | 本需求要求 memory-view 访问必须以 lease handle 形式暴露，handle 必须携带 epoch、version、refcount、expiry、revocation callback；lease 过期、被撤销或 version 不匹配时，memory view 不得继续 attach 或被 GPU/NPU 访问。 | 共享内存 KVCache 安全租约访问 |  |
| L2-OB-PathTrace-030 | P1 | 共享访问与故障控制 | 本需求要求 Connector 为每次 KV access 生成 path decision trace，记录 selected semantic、selected backend、source/destination tier、reason、fallback reason、latency breakdown、bytes 和 descriptor count，支持运维定位“命中了但变慢”的根因。 | KVCache 传输路径可追踪运维 |  |
| L2-MM-MultiConsumer-027 | P2 | 共享访问与故障控制 | 本需求要求 Connector 支持 multi-consumer KV handle：同一 KV object 被多个 decode replica、TP rank 或请求消费时，应通过 refcount、consumer bitmap 和 shared visibility fence 共享一次加载结果，避免多消费者重复远端拉取。 | 共享前缀 KVCache 批量广播 | 组播会导致单个请求的时延上升，这个需求是否有必要？ |
| L2-CONN-DefragPause-031 | P1 | 共享访问与故障控制 | 本需求要求 Connector 层实现租约感知的碎片整理暂停与无感原子更替：当底层执行内存块迁移时，若涉及活跃的 Lease 句柄，必须通过 RCU（Read-Copy-Update）无锁机制延迟迁移，或在 50μs 内原子更替虚拟到物理的指针映射。 | 活跃租约下内存无感整理 |  |
| L2-CONN-WatermarkQuery-032 | P1 | 路径路由与能力探测 | 本需求要求 Connector 将节点内的容量水位提示（Watermark Hints）整合进路径质量探测流中。当目标节点处于 High 水位时，自动拉高该路径的 Cost 评分，触发路由自适应引流，规避过载节点。 | 动态拓扑水位路径规避 |  |
| L2-KV-PublishCommit-033 | P0 | 统一协议与标准接口 | 本需求要求 KVConnector 在 put/get/prefetch/evict 之外补充 publish_prepare、publish_commit、publish_abort 三阶段发布接口：NPU/GPU runtime 产生 KV 后先注册待发布 KV object，只有当 KV data、KVMeta、ready bitmap、checksum 和 placement record 均完成后才允许 commit；abort 时必须回滚半成品对象并释放临时 buffer。 | NPU/GPU 生成 KV 后进入统一 KV 内存池 |  |
| L2-KV-AttachHandle-034 | P0 | 共享访问与故障控制 | 本需求要求 Connector 返回标准 KVAttachHandle，而不是只返回 KV 数据位置；handle 必须包含 object_id、prefix_span、rank_slice、layout_version、device_ptr 或 transfer_plan、ready_bitmap、visibility_level、lease_id、placement_id 和 release callback，使上层 attention runtime 能安全 attach 和 detach。 | 前缀命中后 KV 安全消费 |  |
| L2-KV-ConsumeEligibility-035 | P0 | 本地元数据缓存与快速判定 | 本需求要求 Connector 提供 consume eligibility 查询接口：输入 prefix_hash、semantic identity、deadline、target_device、rank context，返回 CONSUMABLE、WAIT_READY、NEED_LOAD、NEED_COPY_TO_HBM、MIGRATING_RETRY、STALE_RECOMPUTE、MISS 等语义结果，避免 Scheduler 直接解释底层 KV 状态。 | 前缀命中后的可消费状态判定 |  |
| L2-KV-SemanticIdentity-036 | P1 | 统一协议与标准接口 | 本需求要求定义 KVSemanticIdentity 标准结构，至少包含 model_id、model_weight_version、tokenizer_hash、template_version、adapter_id、layout_version、dtype、position_policy、attention_policy、rank_partition、tenant_domain 和 cache_salt；Connector 在返回 hit 前必须完成 identity 兼容性过滤。 | 防止语义不兼容 KV 被误复用 |  |
| L2-KV-StateErrorCode-037 | P0 | 共享访问与故障控制 | 本需求要求 Connector 层定义统一状态错误码，将 L3/L4 返回的 MIGRATING、LEASE_CONFLICT、STALE_VERSION、VISIBILITY_TIMEOUT、REPLICA_DEGRADED、CHECKSUM_FAILED、QUOTA_BLOCKED、BACKEND_BUSY 等状态转换为上层可执行的 retry、wait、load、copy、recompute、partial hit 或 miss 信号。 | KV 状态复杂场景下的快速降级与恢复 |  |
| L2-KV-PartialAttachPlan-038 | P1 | 传输描述与布局协商 | 本需求要求 Connector 支持 partial attach plan：当 prefix 只有部分 block/page 可用时，返回可 attach 的连续 prefix 区间、缺失 suffix 区间、推荐 recompute boundary、所需 block table 拼接信息与 transfer descriptor，支持“命中前缀 attach + 后缀重算”的混合执行。 | 部分前缀命中复用 |  |
| L2-CONN-CostAwareReturn-039 | P0 | 统一协议与标准接口 | 本需求要求 KVConnector 的前缀查询与获取接口实现“算力代偿感知自适应返回协议”：存储池根据底层链路 P99 时延和拓扑质量，自动评估将命中 KV 搬运至目标端（或协商 staging area）的端到端加载耗时；若该耗时超过推理框架设定的重算时延阈值（或导致 TTFT 负收益），存储池内部判定为逻辑 Miss，向推理框架返回统一的 MISS 或 RECOMPUTE 信号，由框架触发本地重算，禁止返回低效、超时的物理块地址。 | 拥塞链路下的前缀命中低效绕行 |  |
| L2-CONN-BufferContract-040 | P0 | 统一协议与标准接口 | 本需求要求明确划分推理框架执行内存（Execution HBM）与存储池存储资源（Storage Pool）的权责边界。统一存储池不负责维护推理运行时的算力准入状态与本地卡上执行 HBM 剩余容量校验；存储池接口采用“拉取至指定指针（Pull to Provided Device Pointer）”契约，由上层推理框架独立确保目标 device_ptr 分配的可用性与安全性，存储池在检测到目标地址不可写或引发寻址错误时，仅通过标准错误码进行感知与上报，防止存储池逻辑过度侵入算力内存管理。 | 算力执行内存与存储池内存管理解耦 |  |
| L2-CONN-SWP-XFER-003 | P2 | 卸载/加载与预取优化 | 本需求要求实现KVCache Block的批量聚合传输能力，将多个非连续Block的批量memcpy，将当前for循环逐Block传输模式替换为单次批量DMA提交，显著提升PCIe带宽利用率并降低CPU launch overhead。 | Swap批量传输优化 |  |

## L3 传输管理层

| 需求唯一标识 | 优先级 | 核心模块 | 需求描述 | 重点针对场景 |
| --- | --- | --- | --- | --- |
| L3-MC-POOL-GLOBAL-001 | P0 | 统一存储池与分层管理 | 本需求要求实现Mooncake集群级统一KVCache内存池，维护全集群内存注册表，支持节点动态加入/退出，提供基于RDMA的跨节点KVCache直接读取路由，以及两级索引（本地 + 全局），将集群KVCache内存利用率提升至≥90%。 | 集群KVCache统一管理 |
| L3-MS-Tiering-038 | P0 | 统一存储池与分层管理 | 本需求要求实现多级 KV tiering manager：根据 active/warm/cold 状态、reuse probability、load cost、memory rent 和 SLO，将 KV 在 HBM、本地 DDR、远端 DDR、本地 SSD、远端 SSD/object 之间 提级或降级。 | KV 热存储容量扩展 |
| L3-MC-HIER-STORE-002 | P1 | 统一存储池与分层管理 | 本需求要求实现KVCache三层分级调度器，按访问时间将KVCache分配到DDR（热，<1min）/本地SSD（温，1-60min）/远端SSD（冷，>1hr）三层存储，支持后台异步下沉（目标≥2GB/s）和异步预热，以及NPUDirect Storage（File）实现SSD到NPU HBM的零CPU拷贝直读路径。 | KVCache三层冷热分级 |
| L3-MS-CostEviction-039 | P1 | 统一存储池与分层管理 | 本需求要求实现KV提级/降级算法，使用多维因素决定保留、降级或驱逐 KV：score 至少包含 saved prefill time、future reuse probability、transfer cost、interference cost、memory rent 和 tenant priority。 | KVCache 成本感知淘汰与降级 |
| L3-MC-POOL-ALLOC-005 | P2 | 统一存储池与分层管理 | 本需求要求为Mooncake统一内存池实现NUMA感知的KVCache Block分配器，通过查询NPU与CPU NUMA节点的亲和关系，优先将KVCache Block分配到最近NUMA节点的DDR内存（numa_alloc_onnode），将Cross-NUMA KVCache访问比例从当前>50%降至<10%。 | NUMA亲和性KV分配 |
| L3-MC-UBAllocator-049 | P2 | 统一存储池与分层管理 | 本需求要求实现 UB pooled memory 专用 KV allocator：采用 offset-based addressing、hugepage/page coloring、NUMA/fabric locality、free-list、defrag 与 quota 控制，将 UB pool 从裸内存资源变成可管理 KV tier。 | UB 共享 KV 热存储池管理 |
| L3-MS-DirectoryMirror-036 | P0 | 前缀目录与元数据平面 | 本需求要求每个推理节点维护 节点级目录镜像，使确定 miss 和热命中尽量在本地完成，避免分布式 directory RTT 进入 TTFT 主路径。 | 节点级 KVCache 目录镜像加速 |
| L3-MS-MetadataDataPlane-035 | P0 | 前缀目录与元数据平面 | 本需求要求将 KV metadata plane 与 KV data plane 物理和逻辑隔离：prefix directory、manifest、ready bitmap、placement summary 优先放 UB/C2C/local DDR；大块 KV data 走 RDMA/DMA/GDS/SSD 路径。 | KVCache 元数据热路径加速 |
| L3-MC-PFX-IDX-003 | P1 | 前缀目录与元数据平面 | 本需求要求将Mooncake的前缀元数据查询路径从基于TCP的etcd/Redis改造为基于RDMA one-sided READ的直接内存访问，通过一致性哈希分片保证单跳路由并消除多跳开销，同时建立热点key本地副本实现μs级本地命中，将分布式前缀元数据查询P50时延从5-20ms降至100μs以内。 | 前缀命中判断加速 |
| L3-MC-PFX-REPL-004 | P1 | 前缀目录与元数据平面 | 本需求要求为Mooncake分布式前缀索引实现基于一致性哈希环（+100 Virtual Nodes/物理节点）的均衡分片机制，并为每个分片提供N=2副本，通过RDMA heartbeat（1s周期）检测故障并在5s内完成主备切换，消除单点故障导致的大规模KVCache查询不可用。 | 分布式索引高可用 |
| L3-MS-MultiReplicaDirectory-037 | P1 | 前缀目录与元数据平面 | 本需求要求全局 KV directory 支持同一 KV object 的多语义副本记录：包括 UB/C2C memory view、local DDR、remote DDR RDMA source、SSD segment、object key，并维护 version、visibility、hotness 与 cost metadata。 | 多副本 KVCache 全局目录管理 |
| L3-PM-HitQuality-023 | P1 | 前缀目录与元数据平面 | 本需求要求 Connector 在返回 prefix hit 时同时返回 hit quality metadata，包括所在 tier、bytes、layout、queue depth、replica count、estimated load latency、visibility state，使 Scheduler 能判断该 hit 是否能在 TTFT budget 内转化为 usable hit。 | 命中 KV 加载价值评估 |
| L3-SE-PolicyEngine-032 | P0 | 语义策略与路径管理 | 本需求要求 Transfer Manager 实现 semantic policy engine：根据 KVAccessIntent、KV size、deadline、reuse value、topology、telemetry、tier state 自动选择 memory view、bulk transfer、stream object 或 recompute 路径。 | 统一 KVCache 存储池智能路径选择 |
| L3-SE-UnifiedDescriptor-018 | P0 | 语义策略与路径管理 | 本需求要求定义统一 KV access descriptor，显式支持四类访问语义：MEMORY_VIEW、BULK_TRANSFER、STREAM_OBJECT、MANAGED_INTENT，覆盖 Mooncake、NIXL、LMCache、HiCache 后端所需的 key、layout、source/destination tier、semantic type、visibility、deadline、fallback、rank slice 等字段，使同一框架侧请求可在不同 connector 后端间无侵入切换。 | 跨框架 KVCache 传输任务统一编排 |
| L3-TRANS-CAP-API-002 | P0 | 语义策略与路径管理 | 本需求要求实现标准化的硬件传输能力查询API（HardwareCapabilityAPI），暴露 backend capability discovery：包括 coherent view、atomic、RDMA/GDR、GDS、CXL view、TCP stream、QoS、fence、NPU DMA、max segment size、registration requirement 等能力，提供get_transport_capabilities()接口返回List[TransportCapability]，每条能力记录包含{type, src_mem_types, dst_mem_types, max_bw_gbps, p50_latency_us, is_available}，覆盖C2C/UB_MEM/UBLINK/RDMA_NPUDIRECT等全部主流传输类型，使L1/L2层无需硬编码平台判断逻辑。 | 平台无关能力感知 |
| L3-TRANS-SEM-ENGINE-001 | P0 | 语义策略与路径管理 | 本需求要求实现统一传输语义引擎（TransferDecisionEngine），在节点启动时通过HardwareCapabilityRegistry探测并注册所有可用传输能力（C2C/UB/RDMA/UBLINK/PCIe等）的BW/latency特性，在每次KVCache传输请求时基于(src_mem, dst_mem, size, dst_count, latency_req)五元组自动选择最优传输原语，并维护动态路由表（100ms更新），支持完整Fallback Chain。 | 多平台统一传输管理 |
| L3-SE-GranularityDispatch-033 | P1 | 语义策略与路径管理 | 本需求要求 Transfer Manager 按数据粒度自动分发访问语义：64B–4KB metadata 优先 memory-view，0.5–8MB KV block/page 优先 bulk transfer，冷 segment/object 优先 stream/object，避免协议与数据粒度错配。 | KV 元数据与 KV 数据分流传输 |
| L3-SE-ViewCopyCostModel-034 | P1 | 语义策略与路径管理 | 本需求要求实现 view-vs-copy cost model：综合 direct view stall cycles、copy-to-HBM cost、expected reuse count、deadline 和 TPOT 敏感性，判断 UB/C2C warm KV 应直接访问还是先 copy 到 HBM。 | 近端热 KV 直访与回填优化 |
| L3-TRANS-TOPO-SENSE-004 | P1 | 语义策略与路径管理 | 本需求要求实现传输路径质量实时感知机制，以100ms为周期采样各路径P50/P99时延（RDMA pingpong +  Event + UB read test），当检测到路径P99超过基线2倍时自动标记degraded并引流至备用路径，通过EWMA预测下5秒路径质量以提前切换，使路径拥塞期间KVCache传输P99恶化比例控制在1.5x以内。 | 动态传输质量适应 |
| L3-MS-HotReplication-040 | P1 | 传输编排与广播 | 本需求要求对公共 system prompt、agent tool schema、RAG 热模板等 hot prefix 自动复制到多个本地 DDR、UB pool 或远端 DDR shard，复制决策由 QPS、p99 latency、queue depth 和 cross-node bytes 触发。 | 热点前缀 KVCache 多副本扩散 |
| L3-TRANS-MUL-ENGINE-003 | P1 | 传输编排与广播 | 本需求要求实现1→N KVCache多播传输优化引擎，通过路由树构建（最大化局部性最小化跳数）和硬件UD组播+软件P2P relay混合模式，使Disaggregated Prefill场景下同一前缀KVCache多节点分发时发送端BW消耗为O(1)。 | 一对多KVCache高效分发 |
| L3-C2C-POOL-SCHED-002 | P2 | 传输编排与广播 | 本需求要求基于NPU Unified Memory访问统计（NPUMemRangeGetAttrute）实现统一内存中KVCache Block的热度感知动态迁移调度，将访问频次超过阈值（默认10次/s）的Block异步预迁移至NPU HBM（NPUMemPrefetchAsync），将冷降温的Block通过madvise释放HBM页面保留在CPU DDR，维持HBM利用率>85%。 | C2C统一内存热度调度 |
| L3-LMC-PRFCH-ENGINE-001 | P2 | 传输编排与广播 | 本需求要求实现基于访问时序滑动窗口（1000条记录）的访问模式预测引擎，当检测到特定prefix_hash在时间窗口内满足频率阈值（5秒内>3次且最近1次<30s前）时，自动触发该KVCache从SSD到DDR的预加热（后台队列，占用NVMe BW≤30%），将SSD→NPU的端到端时延从>150ms降至<30ms（预热命中场景）。 | 冷KVCache预取加热 |
| L3-UB-TOPO-ROUTE-001 | P2 | 传输编排与广播 | 本需求要求集成拓扑发现，构建完整的UBLINK连通矩阵，在KVCache P2P传输决策时优先选择UBLINK路径（bandwidth优先），实现跨NPU直接内存映射和P2P ，在同节点8卡环境下实现KVCache P2P传输带宽≥700GB/s。 | 机内NPU KVCache P2P高速传输 |
| L3-CO-VersionPublish-043 | P0 | 一致性、隔离与QoS | 本需求要求 KV object 发布必须保证数据完整性：统一暴露 completion/visibility contract：区分 local completion、remote visibility、GPU visibility、durable visibility；只有达到请求要求的 visibility level 后，上层才允许 attach KV 或发布 metadata。KV data 写入完成并校验 checksum 后，执行必要 fence/flush，再更新 version 与 ready bitmap；未完成或失败写入不得暴露给任何 lookup 或 attach。 | KVCache 对象一致发布 |
| L3-QO-SemanticQoS-045 | P0 | 一致性、隔离与QoS | 本需求要求 Transfer Manager 建立语义化 QoS 队列：metadata-critical、TTFT prefix load、decode-critical copy、prefetch、background writeback、cold restore 必须分队列、限速和优先级调度，避免后台流量影响前台 TTFT/TPOT。 | 前台推理与后台 KV 任务流量隔离 |
| L3-SC-PoolIsolation-048 | P0 | 一致性、隔离与QoS | 本需求要求多租户 KV pool 实现容量、带宽、metadata namespace、KV key namespace、encryption domain 和 audit log 隔离；任一租户的 UB/RDMA/SSD KV 使用不得对其他租户造成不可控干扰或越权访问。 | 多租户统一 KV 存储池资源隔离 |
| L3-OB-PerPathTelemetry-047 | P0 | 全路径观测与故障追踪 | 本需求要求 Transfer Manager 按路径持续导出 telemetry：包括 UB/C2C view latency、RDMA bandwidth、RDMA CQ latency、copy engine queue、SSD/GDS latency、TCP p99、retry、throttle、fault 和 fallback count。 | KVCache 全路径性能观测 |
| L3-FT-FallbackTrace-046 | P1 | 全路径观测与故障追踪 | 本需求要求为每次路径降级生成 fallback causality trace：记录 view→bulk、bulk→stream、bulk→recompute 或 RDMA→TCP 的触发原因、耗时、丢失收益、影响请求和关联硬件 telemetry。 | KVCache 降级链路故障追踪 |
| L3-MS-UBC2CTier-055 | P1 | 全路径观测与故障追踪 | 本需求要求 UB/C2C memory tier 首先承载KVCache相关的元数据，譬如 prefix metadata、directory mirror、manifest、ready bitmap、warm KV staging，而不是直接承载大规模 decode-active KV；若用于 direct KV view，必须由 view-vs-copy cost model 判定。 | UBlink-C2C 近端热 KV 存储扩展 |
| L3-MC-CompactionEngine-056 | P0 | 统一存储池与分层管理 | 本需求要求实现集群/节点双级 KVCache 内存紧凑引擎（Compaction Engine）。当 HBM/DDR 外部碎片率超过 30% 时自动触发，采用内存屏障（Memory Barrier）技术异步聚合非连续空闲 Block，将碎片率平抑至 10% 以内。 | 长周期运行内存紧凑整理 |
| L3-MC-IntelligentMigration-057 | P0 | 统一存储池与分层管理 | 本需求要求设计三级水位（Low-70%, High-85%, Critical-95%）监测与智能迁移决策引擎。达到 High 水位时，根据 KV 成本淘汰评分（Saved Prefill Time、QPS、租户优先级）异步批量将 Warm/Cold KVCache 换出至本地 DDR 或 SSD；达到 Critical 水位时，强制拓宽换出带宽并限制换入。 | 跨异构介质智能主动搬移 |
| L3-MS-KVObjectStateMachine-058 | P0 | 统一存储池与分层管理 | 本需求要求统一 KV 内存池为每个 KV object 建立显式对象状态机，至少覆盖 INIT、WRITING、COMMITTED、READY、ACTIVE_ATTACHED、PREFETCHING、LOADING、MIGRATING、COMPACTING、EVICTING、TOMBSTONE、FAILED、QUARANTINED 状态；所有 publish、lookup、load、attach、detach、evict、migrate、invalidate 操作必须通过状态机完成合法迁移。 | KV 全生命周期状态一致性管理 |
| L3-MS-ReplicaPlacementState-059 | P0 | 前缀目录与元数据平面 | 本需求要求同一 KV object 的每个副本独立维护 placement state，至少包含 tier、node_id、device_id、offset/extent、layout、visibility_level、queue_state、replica_health、version、last_access 和 migration_token；目录查询必须返回对象状态与副本状态的组合结果，而不是只返回存储地址。 | 多副本 KV 的位置与状态管理 |
| L3-MS-ConsumeEligibility-060 | P0 | 前缀目录与元数据平面 | 本需求要求 L3 元数据平面实现可消费判定函数，根据 KV object state、replica placement state、ready bitmap、version、lease、semantic identity、layout compatibility、deadline 和目标设备，判断该 KV 是否可被 prefix hit 消费，并返回 CONSUMABLE、WAIT、LOAD、COPY、DIRECT_VIEW、RECOMPUTE、MISS 等明确动作。 | Prefix hit 后能否转化为 usable hit |
| L3-SE-PlacementResolver-061 | P0 | 语义策略与路径管理 | 本需求要求实现 placement resolver：当同一 KV object 存在 HBM、本地 DDR、远端 DDR、本地 SSD、远端 SSD/object、UB/C2C view 等多个副本时，根据 tier、传输代价、当前水位、链路质量、queue depth、visibility、lease 冲突和 TTFT deadline 选择最优消费位置，并生成 load/view/copy/recompute 计划。 | 多位置 KV 副本的最优读取源选择 |
| L3-MS-MigrationInterlock-062 | P0 | 统一存储池与分层管理 | 本需求要求在 KV 正处于 MIGRATING、COMPACTING、EVICTING 等状态时提供访问互锁机制：若已有 active lease，则延迟迁移或执行 copy-on-migrate；若查询命中迁移中对象，则返回 WAIT、OLD_REPLICA_READ、NEW_REPLICA_READ、RETRY 或 RECOMPUTE 策略，禁止返回悬空地址或半迁移副本。 | KV 升降级/碎片整理期间的安全消费 |
| L3-CO-AttachDetachLease-063 | P0 | 一致性、隔离与QoS | 本需求要求统一 KV 内存池实现 attach/detach lease：KV 被消费前必须获取 read lease 或 attach lease，lease 期间禁止物理释放、非法覆盖或不可见迁移；detach/release 后递减 refcount，并允许后台 evict、compact 或降级。 | Decode 使用 KV 期间防止被迁移或释放 |
| L3-CO-VisibilityReadyBitmap-064 | P0 | 一致性、隔离与QoS | 本需求要求每个 KV object 维护 layer/page/rank 粒度 ready bitmap 与 visibility state，区分 local write complete、device visible、remote visible、durable visible；只有达到消费方所需 visibility level 的 KV block/page 才允许进入 attach plan。 | KV 写入完成与可见性边界控制 |
| L3-CO-PublishCommitLog-065 | P0 | 一致性、隔离与QoS | 本需求要求统一 KV 内存池实现 KV publish commit log：KV data 写入、metadata 写入、ready bitmap 更新、checksum 生成、version 发布、directory 可见化必须按可恢复顺序执行；节点异常后可根据 commit log 判断对象是可恢复、可丢弃还是需 quarantine。 | KV 发布过程异常恢复 |
| L3-MS-InvalidationTombstone-066 | P0 | 前缀目录与元数据平面 | 本需求要求全局 prefix directory 支持 version invalidation、namespace invalidation 和 tombstone 机制；当 model、tokenizer、template、adapter、layout、tenant salt 或安全域变化时，旧 KV 必须被批量失效，已删除或失败对象必须保留 tombstone/epoch，防止旧副本被目录镜像重新暴露。 | 旧 KV 副本复活与 stale hit 防护 |
| L3-MS-StaleHitGuard-067 | P0 | 前缀目录与元数据平面 | 本需求要求 L3 元数据查询结果必须携带 version、epoch、directory_generation 和 stale 标记；本地目录镜像或缓存命中时必须校验 generation，不得把 stale placement 作为可消费结果返回给 Scheduler 或 Connector。 | 本地元数据缓存过期导致误命中 |
| L3-CO-ReplicaQuarantine-068 | P1 | 一致性、隔离与QoS | 本需求要求对 checksum 错误、visibility 未达标、layout 不兼容、version 冲突或迁移失败的 KV replica 标记为 QUARANTINED；隔离副本不得参与 prefix lookup、placement resolver 或 attach，直到后台修复、重建或删除。 | 异常 KV 副本隔离 |
| L3-MS-PrefixDirectorySchema-069 | P0 | 前缀目录与元数据平面 | 本需求要求设计面向 TTFT 关键路径的 prefix directory entry 结构，entry 至少包含 prefix_hash、prefix_span、semantic identity digest、object_id list、continuous block range、tier summary、best placement summary、version、ready summary、estimated_load_cost 和 hit_quality，使一次查询即可得到“能否消费”和“如何消费”的核心信息。 | 前缀匹配元数据结构设计 |
| L3-MS-HotLocalIndex-070 | P0 | 前缀目录与元数据平面 | 本需求要求每个推理节点维护 hot local prefix index，采用 fast fingerprint + semantic digest + placement summary 的两级结构，使确定 miss、热 prefix hit、stale 过滤尽量在本地完成；远端目录查询只在本地无法确认时触发。 | Prefix lookup 避免远端 RTT 进入 TTFT |
| L3-MS-RangeBatchLookup-071 | P0 | 前缀目录与元数据平面 | 本需求要求 prefix directory 支持 range/batch lookup：一次输入 block hash vector、radix span 或 page range，返回最长连续可消费区间、不可消费区间、候选 placement 列表和推荐 load/recompute 分界，避免逐 block 查询造成 metadata RTT 风暴。 | 长前缀批量命中判定 |
| L3-SE-QueryPlanFastPath-072 | P0 | 语义策略与路径管理 | 本需求要求 L3 查询接口返回 QueryPlan，而不是原始元数据；QueryPlan 必须包含 match_span、consume_action、source_placement、transfer_descriptor、attach_requirement、fallback_action 和 expected_interference，使 L1/L2 不需要多轮查询即可做调度决策。 | TTFT 关键路径上的单次查询决策 |
| L3-MS-DirectoryShardPolicy-073 | P1 | 前缀目录与元数据平面 | 本需求要求 prefix directory 支持按 semantic identity、prefix hash、tenant/domain、model namespace 进行分片，并对 hot prefix 支持本地镜像和多副本目录，避免公共 system prompt、agent tool schema、RAG 模板等热点 prefix 造成单点目录瓶颈。 | 热点前缀目录分片与镜像 |
| L3-MS-MetadataCompactLayout-074 | P1 | 前缀目录与元数据平面 | 本需求要求 prefix metadata 采用紧凑内存布局，将 TTFT 快路径字段与冷字段分离；快路径字段包括 fingerprint、object_id、best tier、ready summary、version、cost hint，冷字段包括完整 manifest、审计、历史访问和多副本详情，降低本地查询 cache miss 和序列化开销。 | 前缀元数据查询结构优化 |
| L3-MC-KVBlockStorageSchema-075 | P0 | 统一存储池与分层管理 | 本需求要求统一 KV 内存池定义 KV block/page 存储 schema，至少包含 model layout、layer range、head range、token range、rank slice、dtype、scale metadata、physical extent list、alignment、checksum 和 compression flag，使 HBM/DDR/SSD/远端节点中的 KV 具备统一可解释结构。 | KV 块跨介质统一存储结构 |
| L3-MC-ExtentManifest-076 | P0 | 统一存储池与分层管理 | 本需求要求为每个 KV object 维护 extent manifest，将逻辑 KV page/block 映射到物理 HBM/DDR/SSD/RDMA/UB extent；manifest 必须支持非连续物理块、连续逻辑区间、cross-layer block、page-first layout 和分层副本记录。 | 非连续 KV 块存储与批量传输 |
| L3-MC-PageExtentAllocator-077 | P1 | 统一存储池与分层管理 | 本需求要求统一 KV 内存池实现 page/extent allocator，支持按 layer、page、rank、tier 分配物理空间，并支持 split、merge、coalesce、relocate 和 defrag hint，避免长期运行后小块碎片破坏 bulk descriptor 合并效率。 | 长周期运行下 KV 存储碎片控制 |
| L3-MC-LayoutTransformPlan-078 | P1 | 统一存储池与分层管理 | 本需求要求当源 KV layout 与目标 attention runtime layout 不一致时，统一 KV 内存池必须生成 layout transform plan，明确是否需要 permute、copy-to-HBM、cross-layer regroup、dtype/scale 转换或直接拒绝复用。 | HND/NHD/page-first/cross-layer 布局兼容 |
| L3-SE-DescriptorFromManifest-079 | P0 | 语义策略与路径管理 | 本需求要求 Transfer Manager 能根据 extent manifest 自动生成 scatter-gather bulk descriptor，并按 max descriptor count、alignment、min transfer granularity、target tier 和 backend capability 进行 coalescing，减少逐 block 传输提交开销。 | 从 KV 存储结构自动生成高效传输描述符 |
| L3-CO-KVOwnershipGC-080 | P0 | 一致性、隔离与QoS | 本需求要求统一 KV 内存池维护 KV object owner、producer、consumer、lease holder、replica owner 和 GC owner；当 producer 崩溃、consumer 超时、request abort、节点下线或 lease 过期时，系统必须能识别 orphan KV 并执行回收、降级、重建或 quarantine。 | KV 对象所有权与异常回收 |
| L3-MS-StateAwarePrefetch-081 | P1 | 传输编排与广播 | 本需求要求预取引擎必须感知 KV object state 和 placement state：对 READY 远端副本可预取，对 MIGRATING 副本延迟或改选源，对 EVICTING 副本禁止预取，对 TOMBSTONE/FAILED 副本直接返回 miss。 | 状态感知的 KV 预取 |
| L3-MS-StateAwareEviction-082 | P0 | 统一存储池与分层管理 | 本需求要求淘汰/降级决策必须检查 active lease、refcount、ready bitmap、replica count 和 tombstone 状态；ACTIVE_ATTACHED 或唯一可用 READY 副本不得被直接释放，必须先完成副本创建、lease 结束或降级保护。 | 防止活跃 KV 被误淘汰 |
| L3-OB-KVStateTrace-083 | P1 | 全路径观测与故障追踪 | 本需求要求为每个 KV object 记录状态迁移 trace，至少包含 publish、commit、lookup、load、attach、detach、migrate、evict、invalidate、quarantine 的时间、触发方、旧状态、新状态和失败原因，支撑“命中了但无法消费”的根因定位。 | KV 状态迁移可追踪 |
| L3-CTRL-KVInspectAPI-084 | P2 | 全路径观测与故障追踪 | 本需求要求提供 KV object inspect 管理接口，可查询 object_id/prefix_hash 对应的 semantic identity、状态机状态、placement 列表、manifest、lease/refcount、version、ready bitmap、last access 和当前推荐消费路径。 | 统一 KV 内存池运维排障 |
| L3-CO-RefCountLifecycle-086 | P0 | 一致性、隔离与QoS | 本需求要求统一存储池的分层流转与驱逐引擎必须复用或集成上层推理框架的活跃引用计数（Active Reference Count）机制。对于驻留于 HBM/DDR 的 KVCache 块，当收到租约到期或强行换出信号时，系统必须校验其 refcount；若 refcount > 0（表明当前仍有活动推理 Batch 正在 attach 或消费），系统必须将该块置于 EVICTING 锁定状态，绝对禁止执行物理空间释放、覆盖或降级换出，只有当引用计数降至 0 且底层硬件 Stream 的 Fence 屏障完成后，方可实施物理回收。 | 活动计算流中 KVCache 块的驱逐保护 |
| L3-MS-AtomicPublishVisibility-087 | P0 | 前缀目录与元数据平面 | 本需求要求放弃控制面的三阶段提交（3PC）与分布式 WAL 日志，数据平面全面拥抱单写者最终一致性架构；但为了确保元数据匹配的正确性，元数据平面必须实现“前缀索引原子化可见性流水线（Atomic Metadata Publication Pipeline）”。当 KVCache 生成并写入时，其额外属性（包含模型 ID、版本、Tokenizer 属性、Layout 架构等）必须在后台完全写入前缀元数据表单，在全部属性与校验和（Checksum）验证通过前，前缀索引节点在全局检索树中必须处于掩码不可见状态（Masked），严禁提前对部分元数据暴露匹配查询，避免跨请求产生误匹配或空地址引用。 | 高吞吐低时延元数据发布一致性 |
| L3-MS-TTFTIndexLayout-088 | P1 | 前缀目录与元数据平面 | 本需求要求针对 TTFT 关键路径，设计并实现一套高吞吐、超低延迟的前缀匹配元数据检索结构：分布式前缀表必须采用多级分片（Sharding）的一致性哈希架构，且核心 Radix Tree 的前两级拓扑节点必须常驻于非交换（Non-Swappable）的本地 Host DDR 或 NPU 专用侧内存中，结合 SIMD（AVX-512 / ARM Neon）指令集对 Token Span 和哈希键进行硬件级并行比对，确保在微秒级内完成确定性 Miss 或命中判定，将索引查询开销彻底从 TTFT 核心路径中解耦。 | TTFT 关键路径高性能元数据检索 |
| L3-SE-MultiReplicaResolver-089 | P1 | 语义策略与路径管理 | 本需求要求实现多副本位置自适应感知与最优路由路径解析算法（Multi-Replica Path Resolver）：当同一份 KVCache 在集群内存在 HBM、本地 DDR、远端 DDR、SSD 等多个异构节点副本时，路径解析器必须实时汇总硬件拓扑（NVLink / UBLink / RDMA / PCIe）、队列拥塞深度（Queue Depth）和介质读取时延，构建启发式惩罚/奖励代价得分模型，动态选出端到端提取耗时最短的最优源端位置，并将解析延迟控制在 <5μs 内，避免多副本场景下的盲目拉取。 | 异构分布式多副本的最优路径路由决策 |
| L3-CO-MigrationRCULock-090 | P1 | 一致性、隔离与QoS | 本需求要求针对处于冷热升降级或碎片整理过程中的活跃中途迁移（In-Flight Migrating / Tiering）KV 块，实现基于 RCU（Read-Copy-Update）理念的无锁高并发一致性读写切换机制：当某一 KV 块正在被后台引擎复制至新介质时，前台查询与 Attach 动作仍被路由至旧有物理地址，保证读取流绝不发生 Stall 或 Block；在目标端数据完整性校验（CRC）及可见性 fence 触发完成的瞬间，通过原子操作一次性更替元数据指针至新物理 Extent，并进入一轮 Batch 的安全退出期后释放旧空间，确保整个迁移生命周期对推理计算层完全透明。 | 动态升降级与碎片整理期间的并发无感消费 |
| L3-MM-Lifecycle-009 | P0 | 容量估算与生命周期 | 本需求要求实现分布式 KV 生命周期状态机，将 KV block/page 明确建模为 ALLOCATED → LOADING → READY → ACTIVE → OFFLOADING → EVICTABLE → RELEASED/FAILED；异步 send/free、request finished、remote load failure 必须通过状态机完成资源回收与错误恢复。 | KVCache 生命周期一致性管理 |
| L3-CONN-PFX-IDX-005 | P0 | 本地元数据缓存与快速判定 | 本需求要求实现两级前缀索引结构：第一级本地 Filter（FPR<0.1%，内存<50MB）在5μs内快速排除确定Miss，避免不必要的分布式RTT；第二级分布式精确索引仅在第一级Filter未排除时才被访问，将整体前缀检测P50时延从5-20ms降至5μs以内（确定Miss场景）。 | 前缀命中快速判断 |

## L4 底层传输层

| 需求唯一标识 | 优先级 | 核心模块 | 需求描述 | 重点针对场景 |
| --- | --- | --- | --- | --- |
| L4-OL-RegisteredPool-053 | P0 | RDMA与零拷贝传输 | 本需求要求底层传输层建立长生命周期 registered memory pool：HBM、DDR、UB region 应预注册为大块可复用区域，通过 offset suballocation 供 RDMA/GDR/NIXL/Mooncake 使用，避免每次 KV block 传输临时注册。 | 高频 KV 传输注册内存池化 |
| L4-RDMA-P2P-NPU-001 | P1 | RDMA与零拷贝传输 | 本需求要求集成NVIDIA NPUDirect RDMA能力，将NPU HBM注册为RDMA可访问内存区域（v_reg_mr），实现NPU HBM→网卡→目标NPU HBM的零CPU拷贝直传路径，消除当前跨节点KVCache传输中2次CPU参与的内存拷贝（NPU→CPU DDR→网卡→远端网卡→CPU DDR→NPU），将端到端传输时延降低40-60%。 | 跨节点KVCache零拷贝传输 |
| L4-RDMA-MUL-FABRIC-002 | P2 | RDMA与零拷贝传输 | 本需求要求实现KVCache的RDMA层1→N广播传输封装，使发送端带宽消耗O(1)（与接收方数量无关）。 | 共享前缀KVCache批量广播 |
| L4-C2C-UNIFY-POOL-001 | P1 | 统一内存与内存语义访问 | 本需求要求在Kunpeng  NPU平台上，统一内存架构实现NPU+CPU DDR+SSD统一KVCache内存池，通过NPUMemPrefetchAsync和热度感知MemoryAdvisor自适应策略，将单节点KVCache有效访问容量从 HBM alone 扩展至HBM + DDR + SSD，使KVCache容量约束得到根本性缓解。 | CPU/NPU统一内存池 |
| L4-UB-C2C-UNIFY-002 | P2 | 统一内存与内存语义访问 | 本需求要求在支持UBLink C2C互联的平台上，为KVCache构建NPU-CPU统一地址空间访问路径，并通过NPUMemPrefetchAsync实现访问模式自适应预取，使NPU能通过指针直接访问CPU侧KVCache，消除显式NPUMemcpy传输开销，将CPU→NPU KVCache访问时延从2-5ms降至<500ns。 | 内存语义零拷贝KV访问 |
| L4-UB-P2P-FABRIC-003 | P3 | 统一内存与内存语义访问 | 本需求要求对接UBFabric Manager，将UB内存区域注册为全局可访问资源并分配Global Access Pointer（GAP），供远端节点直接通过GAP进行load/store操作访问KVCache，无需RDMA注册和显式传输，实现跨节点KVCache内存语义访问，目标访问时延显著低于RDMA传输语义。 | 跨节点内存语义KV访问 |
| L4-MC-HIER-STORE-001 | P2 | DPU卸载与分层存储I/O | 本需求要求实现KVCache三层分级直出，实现NPU直接访问本地SSD（NPUDirectSSD）、NPU直接出网卡（NPUDirectNIC），提升KVCache在不同介质、不同节点之间的流动效率。 | KVCache多层异构架构 |
| L4-MC-HIER-STORE-002 | P3 | DPU卸载与分层存储I/O | 本需求要求实现KVCache on-the-fly 压缩功能，在从NPU卸载到DDR或SSD时，提供on-the-fly无刷压缩功能。 | KVCache多层异构架构 |
| L4-MC-HIER-STORE-003 | P3 | DPU卸载与分层存储I/O | 本需求要求实现KVCache on-the-fly 解压缩功能，在从DDR或SSD加载到NPU时，提供on-the-fly的解压缩功能。 | KVCache多层异构架构 |
| L4-NET-OFFLOAD-DPU-001 | P3 | DPU卸载与分层存储I/O | 本需求要求将KVCache RDMA传输的控制路径（CQ轮询与完成中断通知Host）、数据路径（INT8量化卸载到DPU FPGA）和路由决策（Mooncake路由表本地化到DPU）卸载至 DPU，使Host CPU的KVCache传输相关开销降低≥70%。 | 传输数据路径CPU卸载 |
| L4-FABRIC-ROUTER-001 | P0 | Fabric能力与统一路由 | 本需求要求实现混合传输Fabric路由引擎（Fabric Router），以(src_mem, dst_mem, size, latency_req, dst_count)为输入，基于实时维护的硬件拓扑数据库（每10min刷新）和传输质量反馈，输出最优传输原语（内存语义或传输语义）和具体执行路径，路由决策时延<3μs，在多平台混合集群中正确路由率达到100%。 | 多语义底层传输统一路由 |
| L4-HW-SemCapTable-050 | P0 | Fabric能力与统一路由 | 本需求要求硬件抽象层输出 semantic capability table：对每条路径声明可支持的链路类型，支持 coherent memory view、atomic、DMA、RDMA、GDS、TCP stream、QoS、fence、persistent visibility、NPU DMA，使上层不会误把所有路径当作普通 read/write。 | 异构 KV 传输硬件能力建模 |
| L4-HW-AddrTrans-051 | P1 | Fabric能力与统一路由 | 本需求要求硬件层为 UB/C2C/NPU/NPU memory view 暴露地址翻译 telemetry，包括 IOMMU/ATS/SMMU/TLB miss、page fault、shootdown、page size 和 first-touch 代价，使 direct view 的尾延迟可观测、可优化。 | 共享内存 KV 访问地址转换优化 |
| L4-CO-FencePrimitive-052 | P0 | 一致性、安全与可靠性 | 本需求要求硬件抽象层提供跨语义 fence 原语，至少覆盖 cpu_to_NPU_visible、rdma_to_NPU_visible、NPU_to_UB_visible、UB_to_rdma_visible，确保不同 fabric 间数据完成与可见性边界明确。 | 跨设备 KVCache 可见性同步 |
| L4-FT-RASErrorMap-061 | P0 | 一致性、安全与可靠性 | 本需求要求硬件层将 UB error、RDMA flush error、NPU page fault、TCP timeout、SSD read error、translation fault 映射为统一 KV object fault code，并触发对应 fallback、recompute、replica invalidation 或 quarantine 策略。 | KVCache 硬件故障统一恢复 |
| L4-QO-TrafficClass-056 | P0 | 一致性、安全与可靠性 | 本需求要求底层为不同 KV 流量建立流量隔离：TTFT prefix load、decode-critical copy、metadata lookup、background writeback、SSD cold restore、TCP fallback 应分 RNIC queue、copy stream、UB switch queue 或线程池隔离。 | KVCache 多类业务流量硬件隔离 |
| L4-SC-ViewProtection-062 | P0 | 一致性、安全与可靠性 | 本需求要求 memory view 访问必须具备硬件级保护：read-only mapping、IOMMU/SMMU domain、tenant key、view revocation、poison isolation、audit log；裸地址不得跨租户或跨安全域直接共享。 | 共享内存 KVCache 安全访问保护 |
| L4-OB-RemoteAccessCounter-057 | P1 | 一致性、安全与可靠性 | 本需求要求 NPU/NPU runtime 暴露 remote-tier access counters：包括 UB/C2C load count、remote memory bandwidth、stall cycles、Lmiss source、copy engine utilization，使系统能判断 direct view 是否拖慢 TPOT。 | 近端共享内存 KV 访问性能观测 |
| L4-CO-PageMigration-063 | P1 | 一致性、安全与可靠性 | 本需求要求底层传输层提供硬件级页迁移（Page Migration）与大页（Hugepage）重组原语，允许在不更改上层虚拟地址（GAP/指针）的前提下，在硬件层原子级完成物理内存页复制与 IOMMU/TLB 刷新。 | 硬件级零拷贝内存重组 |
| L4-QO-MigrationQoS-064 | P0 | 一致性、安全与可靠性 | 本需求要求底层传输层为水位触发的主动迁移流量分配专用硬件通道（如特定的 RNIC Queue、PCIe DMA Ring 或独立拷贝引擎），与前台推理流量（TTFT/TPOT）实现物理隔离，确保大规模 KVCache 跨介质搬移时不挤兑推理带宽。 | 主动迁移流量硬件隔离保障 |
| L4-CO-AtomicRemapPrimitive-065 | P0 | 一致性、安全与可靠性 | 本需求要求底层传输层为 KV 迁移和碎片整理提供原子重映射原语：在物理页复制完成、checksum 校验、目标可见性满足后，支持一次性切换 logical extent 到新 physical extent，并完成必要的 IOMMU/SMMU/TLB 刷新与 fence，保证上层 lease handle 不观察到半迁移状态。 | KV 迁移/紧凑整理期间地址无感切换 |
| L4-CO-ExtentVisibilityFence-066 | P0 | 一致性、安全与可靠性 | 本需求要求底层提供 extent 粒度 visibility fence，覆盖 HBM、DDR、UB/C2C、RDMA remote memory、SSD/GDS buffer 之间的数据完成与可见性转换；L3 ready bitmap 只能在对应 extent fence 完成后置位。 | KV block/page 可见性同步 |
| L4-HW-StorageLayoutCapability-067 | P1 | Fabric能力与统一路由 | 本需求要求底层能力表声明各后端对 extent layout 的约束，包括 alignment、max_sg_entries、max_segment_size、registration granularity、direct-IO granularity、page size、cross-layer block support、zero-copy view support，使 L3 生成的 manifest 和 descriptor 不违反后端限制。 | KV 存储布局与底层能力匹配 |
| L4-QO-StateAwareTrafficClass-068 | P0 | 一致性、安全与可靠性 | 本需求要求底层按照 KV 状态迁移类型建立流量隔离：TTFT_LOAD、DECODE_ATTACH_COPY、PREFETCH、MIGRATION、COMPACTION、WRITEBACK、COLD_RESTORE、REPLICA_REBUILD 应使用独立或可限速的 RNIC Queue、DMA Ring、copy stream、UB queue 或线程池，防止后台状态迁移挤兑前台 attach。 | KV 状态迁移流量隔离 |
| L4-FT-ReplicaIntegrityCheck-069 | P0 | 一致性、安全与可靠性 | 本需求要求底层在 KV replica 写入、迁移、读取和加载过程中提供 extent/page 粒度完整性校验能力，支持 checksum/CRC、长度校验、layout 校验和错误上报；校验失败必须触发 L3 replica quarantine 或 recompute。 | KV 副本静默损坏检测 |
| L4-MC-PersistentExtentHandle-070 | P1 | DPU卸载与分层存储I/O | 本需求要求 SSD/GDS/object tier 中的 KV segment 暴露 PersistentExtentHandle，记录 file/object key、offset、length、alignment、checksum、layout 和 durability state，使 L3 manifest 可以统一描述内存副本与持久化副本。 | SSD/object KV 副本统一寻址 |
| L4-RDMA-RemoteExtentHandle-071 | P0 | RDMA与零拷贝传输 | 本需求要求 RDMA/accelerator-direct RDMA 路径暴露 RemoteExtentHandle，包含 rkey、remote_addr、length、node_id、device_id、registration_epoch、visibility_epoch 和 access permission；当 registered pool 重新注册或迁移时，旧 handle 必须失效。 | 远端 KV 副本安全寻址 |
| L4-UB-DirectViewGuard-072 | P0 | 统一内存与内存语义访问 | 本需求要求 UB/C2C direct view 路径提供 view guard，支持 read-only mapping、lease-bound mapping、revocation、fault report 和 remote access counter；若 direct view 在 active decode path 中触发 page fault、TLB miss 激增或远端 stall 异常，必须向 L3 返回降级信号。 | 近端/远端 memory view 安全消费 |
| L4-CO-DrainAndQuiesce-073 | P1 | 一致性、安全与可靠性 | 本需求要求底层为 RDMA QP/CQ、DMA ring、copy stream、GDS handle、UB mapping 提供 drain/quiesce 原语；当 L3 执行 backend 降级、节点下线、registered pool 重建或大规模迁移时，必须先停止新提交并等待已提交 KV 操作完成或失败。 | 后端资源切换与节点下线 |
| L4-SC-SecureExtentRelease-074 | P2 | 一致性、安全与可靠性 | 本需求要求底层支持 KV extent 安全释放：对跨租户或安全域复用的 HBM/DDR/SSD extent，在释放或重新分配前执行 zero-fill、crypto erase 或 poison isolation，并向 L3 返回 release completion。 | KV 物理空间安全复用 |
| L4-HW-NICSpecConstraint-075 | P1 | Fabric能力与统一路由 | 本需求要求针对底层传输层所依赖的高性能网卡（RNIC）和 DPU 提出硬性硬件规格诉求：支持硬件级 SR-IOV 虚拟通道，且单硬件端口必须提供不少于 4 个具备独立硬件速率限制（Hardware Rate Limiting）的硬件队列对（QP）；网卡固件板载 SRAM 必须满足至少 128K 个并发 QP 上下文的高速缓存（QP Context Cache），防止在多租户、多业务流量复用 4 类经典优先级队列时发生固件上下文频繁换出（Context Thrashing），从而在大规模迁移挤兑下确保底层控制信令与前台加载通道的硬时延边界。 | 大规模集群网络软硬件协同性能兜底 |

## 01_核心模块字典

| 层级 | 核心模块 | 模块说明 |
| --- | --- | --- |
| L1 | 调度与路由控制 | 调度、路由、前缀预算、加载收益判定与多层存储感知调度。 |
| L1 | 前缀索引与命中判定 | GPU/C++ 前缀哈希、RadixTree 加速、前缀检测异步解耦与安全哈希。 |
| L1 | 卸载/加载与预取优化 | Swap 批量传输、流式 KV 加载、Lookahead/投机性预取。 |
| L1 | 容量估算与生命周期 | KV 容量估算与分布式生命周期状态机。 |
| L1 | 驻留策略与局部复用 | Active/Warm 分层、View vs Copy、部分前缀复用。 |
| L1 | 多卡协同与观测治理 | 多卡一致、统一访问意图、专家策略、命中观测与多租户隔离。 |
| L2 | 统一协议与标准接口 | 统一 KVConnector Protocol 与标准数据结构，支持三阶段发布与语义兼容校验，支持拥塞链路自适应返回与算力执行内存解耦。 |
| L2 | 路径路由与能力探测 | Connector 层动态路径路由与传输质量探测。 |
| L2 | 本地元数据缓存与快速判定 | 本地元数据缓存、两级前缀索引、批量前缀查询与可消费状态判定。 |
| L2 | 预取触发与命中评估 | 请求到达阶段主动预取与命中状态利用。 |
| L2 | 传输描述与布局协商 | KV 布局协商、批量传输 descriptor 与 coalescing，支持部分前缀命中复用。 |
| L2 | 共享访问与故障控制 | 多消费者共享、View Lease、fallback 契约、路径追踪，支持标准 attach 句柄与统一状态错误码。 |
| L3 | 统一存储池与分层管理 | 集群统一内存池、分层管理、淘汰与分配，支持对象状态机、迁移互锁、块存储结构与布局转换。 |
| L3 | 前缀目录与元数据平面 | 分布式前缀索引与元数据平面，支持副本状态、可消费判定、最终一致可见性发布与 TTFT 极速检索。 |
| L3 | 语义策略与路径管理 | 统一传输语义与策略引擎，支持最优位置解析、查询快路径、传输描述符自动生成与多副本最优路径解析。 |
| L3 | 传输编排与广播 | 传输编排与广播，支持 UBLink 路由、一对多分发、热点复制与状态感知预取。 |
| L3 | 一致性、隔离与QoS | 一致发布、多租户隔离与 QoS，支持租约、可见性控制、发布日志、副本隔离、GC 回收、销毁驱逐保护与无锁 RCU 迁移。 |
| L3 | 全路径观测与故障追踪 | 全路径性能观测与故障追踪，支持状态轨迹追踪与统一运维排障接口。 |
| L4 | RDMA与零拷贝传输 | RDMA与零拷贝传输，支持远端 Extent 句柄安全寻址与注册内存池。 |
| L4 | 统一内存与内存语义访问 | 统一内存与内存语义访问，支持 CPU/NPU 统一内存池、直访保护与 memory view 消费。 |
| L4 | DPU卸载与分层存储I/O | DPU 传输卸载与分层存储 I/O，支持持久化 Extent 句柄寻址。 |
| L4 | Fabric能力与统一路由 | 硬件能力表、地址转换与混合 Fabric 路由，支持布局能力描述与底层网络软硬件协同 QP 规格约束。 |
| L4 | 一致性、安全与可靠性 | 一致性、安全与可靠性，支持原子重映射、可见性 fence、状态感知流量隔离、损坏检测与安全释放。 |

## 设计目标与技术手段

| 设计目标 | 系统挑战 | 采用的技术手段 | 实现手段 |
| --- | --- | --- | --- |
| 算力成本提升，消除最高 40% 冗余计算，大幅压降 TCO | 盲目共享 KVCache 往往会引发网络风暴，若网络拉取时延（数十毫秒）超过本地重算耗时，不仅无法降低成本，反而会导致昂贵的 NPU 算力挂起空转（Stall） | 通过统一虚拟资源池整合全集群HBM、DDR、SSD空闲存储；通过算力代偿评估拦截无效拉取。 | [1.2 软硬件协同路由决策] 与 [1.3 级联水位强反压准入]：依托 L2-CONN-CostAwareReturn-039 等需求，构建了动态代价感知（Cost-Aware）准入机制。网关实时测算链路 P99 带宽延迟，一旦评估搬运时延超过 recompute_saved_time，立即触发“算力代偿拦截”执行就地重算，绝不允许低质命中拖垮整个集群。<br>[3.1 精细化容量预估]：构建全局视角的分层注册池（HBM/DDR/SSD），实现集群规模的冗余算力与存储的高效置换。 |
| 长文本推理使能，突破物理显存硬限制，使能超大批量Token 无感并发 | 长 Context 推理会瞬间突破 NPU HBM 上限，传统框架被迫极度压低 Batch Size，或引发进程 OOM 崩溃。 | 异构多级分层存储池；HBM多级水位线智能下沉（Warm/Cold KV 异步换出至本地Host内存或SSD）。 | [3.2 冷热状态机流转] 与 [3.4 智能主动搬移决策]：基于 L3-MC-IntelligentMigration-057 落地了极其严密的冷热状态机流转协议（ALLOCATED→ACTIVE→EVICTING→RELEASED）。在 HBM 触及 High-85% 水位时，智能引擎自动计算未来复用概率与驱逐代价（Cost-based Eviction），将 Cold KV 异步下沉至本地 Host DDR 或 PCIe SSD，彻底解除物理显存对 Context 长度的枷锁，释放极其宝贵的显存用于计算。 |
| NPU 极致饱和输出与空间碎片抑制，算力与存储解耦， | 高并发长文本下，显存碎片率迅速飙升。传统的碎片整理（Defragmentation）需要暂停算子（Stop-the-world），导致吞吐暴跌。 | Pull-to-Pointer 契约解耦；基于无锁RCU机制的后台页迁移，隐藏介质延迟避免前台算子Stall。 | [3.3 异步内存紧凑整理] 与 [5.2 租约级无感原子迁移]：首创基于 L3-CO-MigrationRCULock-090 (RCU无锁机制) 的后台物理内存紧凑（Compaction）。搬移数据的同时，前台 Attention 计算算子无锁直读旧地址，在后台校验 CRC 无误的瞬间，通过原子指令一键更替元数据物理指针，实现 100% 隐藏空间整理时延。<br>[1.4 框架协议标准化]：推行 Pull-to-Pointer 契约，彻底将存储集群的复杂拓扑对推理框架（vLLM/SGLang）黑盒化。 |
| 超长前缀微快速匹配，极致压缩TTFT 首字延迟 | 分布式超大规模节点间的状态查询会带来高昂的 TCP RTT 风暴，导致 TTFT（首字延迟）急剧劣化。 | 本地BloomFilter结合内存缓存进行两级过滤；基于单边RDMA的非交换DDR内存高速RadixTree匹配。 | [2.1 微秒级快判] 与 [2.3 一致性哈希路由]：在 L2 网关注入本地 BloomFilter 第一级快判（L2-CONN-PFX-IDX-005），确保绝大多数 Miss 判定在 5μs 内拦截。<br>对于可能命中的前缀，直接通过 L3-MC-PFX-IDX-003 单边 RDMA 读取常驻于非交换内存（Non-Swappable DDR）中的 C++ SIMD 硬件加速版 RadixTree，将百万节点的分布式一致性哈希前缀碰撞开销压低到几十微秒内，彻底消除首字延迟的长尾波动。 |
| 数据极速物理流转与多播组网，消除 CPU 控制面开销 | 百 GB 级别的 KVCache 穿透 PCIe 总线时，如果依赖 Host CPU 的套接字拷贝，将不可避免造成严重的内核态上下文切换。 | Scatter-Gather聚合；NPUDirect (GPUDirect) RDMA 零拷贝通道；1→N 硬件多播分发；DPU离线硬件编解码。 | [4.2 散射收束描述符转换] 与 [4.3 底层零拷贝与 DPU 写回]：框架引擎产出的碎片化 KV Block 经过 L2-OL-BulkDescriptor-025 聚合为 Scatter-Gather 连续描述符。<br>在传输底层，直接调用 L4-RDMA-P2P-NPU-001 (NPUDirect RDMA)，打通 GPU 到网卡的内存映射，实现 100% 绕过 Host CPU 的 Zero-Copy 传输。<br>在多节点复用相同 System Prompt 场景下，激发底层 L3-TRANS-MUL-ENGINE-003 (1→N 硬件多播)，确保源端带宽开销恒定为 O(1)。 |
| 提升软件运行稳定性、多租户隔离与容错自愈 | 在万卡规模下，硬件故障是常态，且背景数据搬运往往会挤占 PCIe 物理带宽，导致前台在跑 Batch 突然卡顿，引发严重的多租户干扰。 | 硬件 QoS QP 限流隔离背景流量；卡间 usable prefix Rank共识；全决策Trace自适应追踪与RAS故障自愈映射。 | [5.1 多租户安全隔离] 与 [5.3 搬移流量的硬件级 QoS 隔离]：基于 L4-QO-MigrationQoS-064，系统在支持 SR-IOV 的网卡固件级划分特定 QoS QP 队列（限制硬件发包速率）。即使后台发生海量冷热数据下沉，也绝对无法抢占前台推理算子的物理带宽。<br>依托引用计数（Refcount）锁定活动块；通过 L1-PD-RankConsensus-013 强制执行 TP 并发卡间可用前缀长度的 Rank 级共识对齐。<br>辅以 [6.1-6.3 遥测与容错自愈]，将底层硬件级 RAS 错误映射为标准架构异常并隔离受损副本，确保推理全链路的白盒化自愈。 |
