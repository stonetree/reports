# 大模型注意力键值缓存 (KVCache) Host 侧存储与调度技术地图

> **版本**：V1.3  
> **面向对象**：一线系统研发团队、AI 基础设施架构师、存储与网络系统专家、技术评审人员  
> **基线对齐**：全面基于 2026 年系统、网络、存储与数据库五大顶会（FAST、NSDI、OSDI、SIGCOMM、SIGMOD 共 17 篇论文）客观事实成果展开，以计算机体系结构第一性原理为锚，揭示上层计算范式剧变对底层存储调度的确定性重塑。

---

## 宣讲开场白：破局 AI 基础设施深水区

> **开篇主讲词 / 架构导语**：  
> “各位专家、各位同仁：  
> 随着大语言模型从单体推理全面迈向 **PD 分离、超长上下文、稀疏路由与自主 Agent** 的全新计算时代，算力基础设施的架构重心正在经历一场深刻的位移——**计算不再孤立发生于加速芯片内部，而是被注意力键值缓存（KVCache）在系统级与集群级的海量流动所深刻重塑**。  
> 面对当前 AI 基础设施领域直面的**三大根源性矛盾**——*算力演进极速与传输阻抗高企之矛盾、海量复用潜力与同步放大瓶颈之矛盾、在线严苛 SLO 与混压长尾抖动之矛盾*，我们立足计算机体系结构的第一性原理，全面识别了 GPU/NPU 上层计算与调度范式变革对 Host 侧存储与调度的确定性驱动机制，深入解构了跨越本机 NUMA、集群 Scale-Out、远端解耦内存、CXL 共享内存池、NVMe SSD 分层存储与动态选路 QoS 的**六大关键问题域**，提炼出了这份**面向大模型时代 Host 侧存储与调度的工业级技术地图**。  
> 这份地图系统梳理了从硬件 DMA 直达、树状中继广播、只读数据解耦、CXL 全局 DAX 编址、用户态 Direct I/O 到物理通道硬隔离等 24 项关键解决技术，为我们构建面向国产 AI 硬件的高性能、低成本、高韧性存储底座提供了坚实清晰的技术全景。”

![大模型注意力键值缓存 (KVCache) Host 侧存储与调度技术地图](KVCache_AI_Infra_Technical_Landscape_Map.png)

---

## 零、专有名词与直白功能释义对照

为了便于跨研发方向团队统一语境，本技术地图涉及的核心专有名词首次出现释义如下：

1. **KVCache**（大模型注意力键值缓存）：大模型自回归生成过程中缓存的历史 Key 与 Value 激活状态张量，用于避免后续 Token 生成时重复计算注意力；
2. **Saved-Prefill**（首字生成预计算节省）：利用已缓存的 KV Cache 避免重复计算输入 Prompt，从而显著降低首 Token 延迟（TTFT）；
3. **TTFT**（Time To First Token，首字生成延迟 / 首 Token 响应时间）：从用户发起请求到生成第一个 Token 的端到端耗时；
4. **TPOT**（Time Per Output Token，每个输出 Token 的生成耗时 / 单字生成延迟）：自回归解码阶段逐字生成的平均耗时；
5. **PD 分离**（Prefill 与 Decode 计算生成解耦架构）：将大模型的输入理解 Prefill 阶段与逐字生成 Decode 阶段分别部署在不同的物理硬件节点上，通过高速网络传输 KVCache；
6. **Chunked Prefill**（分块首字预计算）：将超长 Prompt 切分为若干固定大小的 Chunk 分批输入计算，以平滑显存峰值并实现与 Decode 阶段的细粒度交织调度；
7. **Host CPU 零数据拷贝**（CPU 仅负责下发控制指令，不参与正文数据搬运，Host Payload Touch Bytes 严格为 0）：数据直接在加速卡、远端 DDR、CXL 内存池或 NVMe SSD 之间流转，消除主机 CPU 内存搬运与总线争用；
8. **Direct-View**（远端直读）：直接通过高速总线跨节点读取远端内存中的 KV 数据，不产生本地全量内存拷贝；
9. **Copy-to-HBM**（拷贝到本地显存）：通过 DMA 通道将远端 KV 数据完整搬运至本地高带宽显存（HBM）中；
10. **ViewGuard**（视图租约安全守卫机制）：负责管理远端直读的生命周期与租约有效性，并在远端崩溃或超时时安全捕获异常并主动平滑回退至本地重算；
11. **QueryPlan**（动态选路决策引擎）：在微秒级时间内根据链路状态、算力与上下文长度选择最优数据加载或重算路径；
12. **CostEvaluator**（成本预估模型）：在决策前量化预估各加载路径与重算开销的数学模型；
13. **ConsumeEligibility**（多维度语义一致性校验）：对命中 KV 的模型架构、Tokenizer 词表哈希、Prompt 模板哈希、LoRA 适配器、就绪位与租约有效性等 6 维语义进行微秒级合法性检查，杜绝错误消费；
14. **SemanticQoS**（前后台服务质量保障策略）：通过网络优先级队列映射与应用层微秒级自适应退避，确保后台数据换出与拉取不干扰前台在线推理的 TPOT 尾部延迟；
15. **RCU**（Read-Copy-Update，读-拷贝-更新）：一种无锁读与宽限期延迟释放机制，在后台内存碎片整理与页迁移时保证前台读请求零停顿；
16. **UBMEM**（统一总线内存直通共享协议）：支持跨节点与异构设备间直接共享物理内存地址空间的底层通信协议；
17. **URMA**（通用远程直接内存访问）：用户态的高性能 RDMA 驱动接口与通信协议；
18. **Incast**（多对一突发网络拥塞）：多个发送节点在极短时间内同时向单一接收节点回传数据包，导致交换机出端口缓冲区瞬间耗尽而引发的排队丢包风暴；
19. **CXL**（Compute Express Link，通用计算互联总线）：用于支持 CPU、加速器与内存池之间高带宽、低延迟的缓存一致性互联；
20. **SPDK**（Storage Performance Development Kit，存储性能开发套件）：基于用户态与轮询驱动的高性能 NVMe SSD 存取套件。

---

## 第一部分 AI 基础设施三大根源物理矛盾

学术界与工业界在深入研发与规模化部署长上下文、大吞吐推理集群时，撞上了计算机体系结构的第一性原理物理铁壁。所有技术演进的底层动力，均发端于以下**三大根源性物理矛盾**：

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
│                             AI 基础设施围绕 KVCache 的三大根源物理矛盾                           │
├──────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                  │
│  【矛盾一：算力演进极速 vs 传输阻抗高企之矛盾】                                                    │
│  ├─ 表现：GPU/NPU 计算密集爆发，Prefill/Decode 算力与吞吐指数级提升；但外部网络与总线存在物理瓶颈。│
│  ├─ 冲突：Host 传统网络协议栈握手开销大，CPU 参与数据拷贝侵蚀宝贵算力，陷入“算力等数据、协议税吞噬带宽”。│
│  └─ 承接 Host 问题域：【领域一：本机 NUMA 访存与 CPU 拷贝】+【领域四：CXL 交换池直达与端侧流控】    │
│                                                                                                  │
│  【矛盾二：海量复用潜力 vs 同步放大瓶颈之矛盾】                                                    │
│  ├─ 表现：长文本与多轮 Agent 对话带来高达 98%+ 的历史前缀复用空间，促成机架与集群跨机池化扩展。    │
│  ├─ 冲突：分布式缓存维护热度与链表一致性使控制流量占 86.6%，跨机并发原子锁压制读吞吐 4.3 倍，引发严重性能瓶颈。│
│  └─ 承接 Host 问题域：【领域二：集群 Scale-Out 网络失衡】+【领域三：远端解耦内存一致性税与同步放大瓶颈】│
│                                                                                                  │
│  【矛盾三：在线严苛 SLO vs 混压长尾抖动之矛盾】                                                    │
│  ├─ 表现：大模型在线生成对 TTFT/TPOT 具有严苛的毫秒级 SLO 要求；而海量冷 KV 必须向 SSD 分层沉淀。 │
│  ├─ 冲突：慢速 SSD 读引发严重队头阻塞；后台满载异步换出与前台在线推理共享通道，瞬间击穿 P99 尾部时延。│
│  └─ 承接 Host 问题域：【领域五：NVMe SSD 分层存储与异步流水】+【领域六：多维代价选路与混压硬 QoS】 │
│                                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 第二部分 上层 GPU/NPU 计算范式最新变革对 Host 侧技术的确定性驱动

虽然纯 GPU/NPU 芯片运行侧的内部算子细节不纳入本技术地图，但**GPU/NPU 侧计算模式、调度模式与计算范式的最新变革，是倒逼 KVCache 职责演化及 Host 侧技术革新的第一驱动源**。下表深入解构五大计算范式跃迁对 Host 侧技术的确定性驱动机理：

| GPU/NPU 侧计算/调度范式最新变革 | 对 KVCache 生命周期与流动的本质影响 | 倒逼 Host 侧与 CPU 侧引发的系统级技术革新 |
| :--- | :--- | :--- |
| **1. Prefill / Decode 计算解耦 (PD 分离架构)** | 计算密集型的 Prefill 节点与访存密集型的 Decode 节点物理分离，KVCache 必须跨网络节点高频、大吞吐流式传输。 | 驱动 Host 侧**非对称多路径回源分流**（调度闲置 DE 节点存储网卡回源）、**集群分层树状中继广播**（AITurbo 单份回源接力分发）与**16MB 固定块双向流水转运**。 |
| **2. 分块流式预计算 (Chunked Prefill & 超长上下文)** | 超长 Prompt 切块与 Decode 阶段交织执行，KVCache 呈现波浪式增量写入与长生命周期驻留特征。 | 驱动 Host 侧**分层连续物理块预分配**（消除动态小页伙伴系统碎片）、**SPDK/io_uring 用户态异步直读**（4KB 扇区对齐）与**计算—存储双向语义感知淘汰**。 |
| **3. 原生稀疏注意力与动态路由 (MLA / MoE / Indexer)** | 注意力机制由密集连续计算演化为按需动态稀疏激活（如 DeepSeek-V3），KVCache 访问变为微秒级高频离散寻址。 | 驱动 Host 侧**无锁 RCU 树状元数据索引**（读零停顿）、**内存语义全局纳秒编址直达**（百纳秒直接解引用）与**分裂式元数据追加日志推进**。 |
| **4. 多轮对话交互与自主 Agent 循环工作流** | Agent 呈现“思考-工具调用-再观察”的长程多轮交互，历史前缀命中率高达 98%+，用户思考与打字时间明确存在。 | 驱动 Host 侧**前置弱信号自适应预取**（SYMPHONY 捕获打字等交互信号提前触发迁移）与**未来重用距离感知淘汰**（Bidaw 结合回答长度预测重用距离）。 |
| **5. 端到端静态计算图捕获 (Static CUDA Graph)** | GPU 为消灭算子发射开销采用全图捕获，严格要求计算执行期零外部中断、零动态分配与静态内存常驻。 | 驱动 Host 侧**Host CPU 零数据拷贝控制架构**（Payload Touch 严格归零）、**运行期零 CPU 介入剥离**（ECHO 预分配静态物理地址池）与轻量异步流驱动。 |

---

## 第三部分 Host 侧六大核心领域与 24 项关键解决技术矩阵

围绕三大根源物理矛盾与五大计算驱动源，Host 侧多级存储与调度体系在六大物理层级催生了 24 项关键解决技术（各解决技术统一采用 **“技术名称：简要技术手段说明”** 规范）：

### 3.1 领域一：本机 Host DDR 访存与 CPU 拷贝陷阱（本机 NUMA 层）
- **核心矛盾**：CPU 密集搬运数据 vs 主机算力侵蚀与 NUMA 总线争用。
- **底层根因**：Host DDR 带宽远逊于多卡并发总需求，传统 CPU 参与数据拷贝使 Host Payload Touch 吞噬数百核 CPU 算力；跨 NUMA 远端访存增加 30%~50% 延迟；通用 malloc 产生严重外部碎片与页表开销。
- **关键解决技术**：
  1. **Host CPU 零数据拷贝控制架构**：CPU 严格退守纯控制面与拓扑调度，数据正文由硬件 DMA 通道直达流转，Host Payload Touch Bytes 严格归零。
  2. **NUMA 亲和绑定与大页预分配**：将请求上下文与对应 NUMA 节点 DDR 通道严格绑定，结合 1GB/2MB 静态大页消除跨插槽总线争用与 TLB 抖动。
  3. **分层物理块连续分配器**：废弃通用动态内存分配伙伴系统，按 4KB/64KB 硬件几何对齐直接预分配连续物理 Block，内存碎片率收敛至 0。
  4. **无锁 RCU 树状元数据索引**：采用 Read-Copy-Update 读写无锁机制管理前缀树与物理块映射，前台读零停顿，CPU 控制开销削减 80% 以上。

### 3.2 领域二：集群 Scale-Out 网络失衡与带宽汇聚（集群网络与中继层）
- **核心矛盾**：集群存储网络单源回流打满 vs 计算网络与闲置节点常态空闲。
- **底层根因**：长文本多轮交互下历史 KV 命中率超 98%，传统 PD 架构仅由 Prefill 节点直连存储，引发存储网卡 100% 满载与 Incast 丢包；而 Decode 存储网卡与大容量计算网络闲置；单机私有 PCIe 刚性封闭无法借调。
- **关键解决技术**：
  1. **非对称多路径回源分流**：突破单一节点读存储限制，调度闲置节点空闲存储网卡分流回源，再借助内部充裕的计算网络中继转运。
  2. **集群分层树状中继广播**：热点前缀数据从存储源拉取单份，在计算节点间构建树状接力广播拓扑，将单源拉取网卡出口压力削减 75%。
  3. **固定块双向流水转运**：将跨节点多跳中继切分为 16MB 固定块双向并发流水，中继节点转发延迟完全被网络传输时间掩盖。
  4. **组级协同 I/O 意图暴露**：应用层显式声明批量读写批次，集群调度器统一聚合跨节点相同数据请求，消除存储网络重复拉取风暴。

### 3.3 领域三：远端 DDR 内存池化一致性税与同步放大瓶颈（远端解耦内存层）
- **核心矛盾**：海量跨节点远端内存复用 vs 控制面同步元数据流量吞噬有效带宽。
- **底层根因**：远端 DDR 需经微秒级 RDMA 访问；传统分布式缓存淘汰为维护热度与链表，元数据与热度更新占用 86.6% 网络带宽 (正文仅占 13.4%)；跨机并发原子 CAS 导致正常读吞吐暴跌 4.3 倍。
- **关键解决技术**：
  1. **只读不可变数据与元数据本地化**：远端共享池仅存放不可变只读 KV 张量，目录索引与租约状态在本地 Host 闭环，从物理上消除跨机写冲突。
  2. **组级粗粒度生命周期管理**：数据正文访问保持单对象细粒度直达，淘汰回收与租约更新按组 (如 64 个) 批量处理，控制面管理通信骤降 90%。
  3. **本地热度累积与批量原子回刷**：节点本地累加访问计数，跨机同步时单次 8B 原子指令打包多个对象批量回刷，原子通信发射频率降低 8 倍。
  4. **追加式共享日志与异步推进**：远端内存采用单调追加写入日志，后台异步推进元数据清理与垃圾回收，杜绝前台关键路径同步停顿。

### 3.4 领域四：CXL 交换式内存池直达与端侧流控（CXL 互联与交换层）
- **核心矛盾**：高频细粒度解引用存取 vs 交换网络端口争用与硬件一致性限制。
- **底层根因**：传统网络 RPC 协议栈存在严重软硬件开销 (描述符与中断占 75% 耗时)；CXL 交换网络硬件仅支持链路级信用流控，突发大吞吐霸占端口引发严重队头阻塞；CXL 硬件一致性区受物理面积制约仅数十 MB。
- **关键解决技术**：
  1. **全局统一内存编址与直通映射**：利用 CXL 交换网络将共享内存池映射为全局统一虚拟地址空间，CPU 直接解引用直达访问，时延降至百纳秒级。
  2. **发起者角色判定软件一致性**：根据读写发起者角色在 Host 软件层定向下发精确的 Cache Flush/Invalidate，绕过昂贵的硬件全局侦听总线税。
  3. **分裂式元数据与非一致性内存拓展**：将高频原子锁留在硬件受限一致性区，庞大前缀树移入大容量非一致性 CXL 内存，元数据换出开销削减 8 倍。
  4. **端侧核心无状态流控传输**：通过读取 Host CPU 性能计数器估算真实内存流量需求，在主机端主动调节线程注入配额，根除交换机队头阻塞。

### 3.5 领域五：NVMe SSD 分层存储沉淀与异步流水（NVMe SSD 分层层）
- **核心矛盾**：超大容量 SSD 低成本沉淀 vs 慢速介质读延迟与队头阻塞击穿 SLO。
- **底层根因**：SSD 读延迟比 DDR 慢两个数量级；传统内核 Page Cache 存在双重拷贝与锁争用；缺乏介质感知调度时，慢速 SSD 读取占死队头引发响应恶化 3.8 倍；冷热驱逐策略脱离长文本复用规律。
- **关键解决技术**：
  1. **SPDK/io_uring 用户态异步直读**：绕过内核文件系统与 Page Cache，基于用户态轮询驱动直接提交 4KB 对齐 NVMe 描述符，CPU 开销降低 70%。
  2. **层级解耦双队列与响应比优先调度**：将请求拆分为就绪队列与异步准备队列，慢速 SSD 读取按响应比动态升权，消除慢 I/O 对内存命中的队头阻塞。
  3. **计算—存储双向语义感知淘汰**：存储层向上暴露 KV 介质层级，计算调度向下传递回答长度与重用距离下界，多层缓存未命中率降低 57%~70%。
  4. **弱信号前置自适应预取**：捕获用户输入、打字或 Agent 调用拓扑前置信号，在请求到达前数秒提前触发迁移，将多 GB 搬运剥离关键路径。

### 3.6 领域六：多维代价感知动态选路与混压隔离（动态选路与 QoS 层）
- **核心矛盾**：动态多介质带宽波动引发性能反噬 vs 混压 I/O 抖动击穿尾延迟。
- **底层根因**：各级介质有效带宽与拥塞程度动态剧烈波动，静态拉取在低带宽下耗时反超本地重算 (时延反噬)；后台满载换出瞬间抢占 Host 内存控制器与网卡队列，在线推理 TPOT 尾部延迟断崖式恶化。
- **关键解决技术**：
  1. **常数时间动态选路决策引擎**：实时遥测各级链路带宽与计算负载，沿延时包络在常数时间内判定直读、加载或本地重算，杜绝性能倒挂。
  2. **硬件多优先级队列网络硬隔离**：在线推理通信映射高优先级硬件队列，后台换出搬运映射低优先级，并配合动态整形，确保前台网络零排队。
  3. **物理内存通道硬隔离与带宽保留**：通过内存控制器将前台实时业务与后台大吞吐搬运严格绑定至不同物理通道，彻底消除 99% 的内存总线争用。
  4. **视图租约安全守卫与异常平滑回退**：远端直读与解耦内存建立租约机制，在远端故障、网络中断或超时时安全捕获异常并主动平滑回滚至本地重算。

---

## 第四部分 17 篇顶会论文 Host 侧优化技术客观解构矩阵

围绕上述三大矛盾与六大问题域，2026 年各大系统、存储与网络顶会涌现出一批极具创新性的 Host/CPU 侧优化技术。下表系统解构这 17 篇论文在 Host 侧的核心贡献与量化实测成果：

| 顶会与论文 | 归属问题域 | Host 侧核心技术抽象与设计机理 | 关键量化实测成果 (Empirical Benchmarks) | 对 Host 侧体系架构的核心启示 |
| :--- | :--- | :--- | :--- | :--- |
| **DirectKV**<br>*(OSDI '26)* | 领域一<br>(本机 DDR) | ① **Host 内存直通旁路设计**：废除 Host CPU 参与的数据拷贝，Host Payload Touch 严格归零<br>② **异步双缓冲流水编排**：由轻量 Host 驱动异步下发硬件描述符，实现数据搬运与计算完全掩盖 | 慢速总线重复搬运量由 33.5GB **骤降至 0.4GB (减少 98.8%)**；GH200 下端到端延迟缩减至分离版本的 **1/3~1/2.5** | 确立了 Host CPU 严禁触碰数据正文的第一铁律，证明了 Host 端必须退守纯控制指令下发职责。 |
| **Strata**<br>*(OSDI '26)* | 领域一<br>(布局解耦) | ① **Host 侧 Page-First 连续布局**：在 Host 内存与 SSD 侧统一组织为物理连续的大块布局，单次传输打满总线<br>② **离散请求批量汇聚**：Host 控制器将细粒度分散块聚合为硬件 Scatter-Gather 描述符 | 硬件物理总线有效带宽达到 **48 GB/s**（打破离散小页仅 5%~22% 的总线饥饿）；服务吞吐提升最高达 **5 倍** | 解决了上层细粒度小页逻辑视图与底层 Host 总线必须物理大块连续传输之间的经典阻抗矛盾。 |
| **ECHO**<br>*(OSDI '26)* | 领域一<br>(控制解耦) | ① **Host 解释型控制面剥离**：将原本驻留在 Host Python 的高频内存分配释放逻辑彻底剥离，杜绝打断硬件流水<br>② **预先注册静态物理空间**：Host 侧仅在启动期预分配大颗粒物理内存池，运行期零 CPU 介入 | 内存分配释放时延**降低 1.6~1.9 倍**；长文本吞吐提升最高达 **2.1 倍**；彻底消除了 CPU 线程上下文切换开销 | 证明了运行期 Host 控制面介入频率越低，系统流水线执行越平稳，静态预分配是降低控制开销的关键。 |
| **DualPath**<br>*(SIGCOMM '26)* | 领域二<br>(集群网络) | ① **非对称多路径回源调度**：调度 Decode 节点常态闲置的存储网卡参与回源，再借助高速网络中继至 Prefill 节点<br>② **硬件 QoS 虚拟通道隔离**：Host 网卡层配置不同优先级 VL，严格保证在线计算与数据搬运互不干扰 | 存储网卡 Max/Avg 偏斜由 1.53 **降至 1.18**；集群可达峰值利用率提升至 **84.7%**；1152 卡集群吞吐**提升 1.87~1.96 倍** | 打破了单节点独立回源的刚性假设，将集群内闲置的 Host 网卡资源聚合为可弹性调度的公用传输池。 |
| **TurboBus**<br>*(SIGCOMM '26)* | 领域二<br>(总线池化) | ① **跨总线中继协同**：单机内利用高速跨卡互连，借用邻居空闲的 Host PCIe 链路并发访问 Host DDR<br>② **16MB 固定块双向并发流水**：切分固定块大小并在 Host 端建立双向流动，中继跳延迟完全被传输掩盖 | 将整机利用率不足 27% 的空闲 PCIe 链路完全激活；跨卡多链路聚合带宽提升数倍，突破单通道物理限制 | 证明了通过 Host 侧拓扑调度，无需升级硬件即可将孤立的单机点对点链路重构为共享总线池。 |
| **AITurbo**<br>*(FAST '26)* | 领域二<br>(双网协同) | ① **组级 I/O 意图显式暴露**：应用向 Host 暴露组读写批次，Host 聚合请求并消除存储网络重复拉取<br>② **单源回源与树状流水广播**：多节点相同数据仅由单源拉取一次，随后在集群高速网络内流水广播接力分发 | Checkpoint 写入提速 **3.9~58.8 倍**；冷读取平均 TTFT **降低 23%**；透明去重削减高达 **47.2%** 的网络 I/O 耗时 | 证明了 Host 存储系统必须具备全局协同意识，利用计算网络的充裕带宽为受限的存储网络分流。 |
| **FORGE**<br>*(OSDI '26)* | 领域三<br>(远端 DDR) | ① **组级粗粒度管理解耦**：访问保持对象细粒度，生命周期淘汰与状态维护按组（如 64 个）批量执行<br>② **本地热度计数与批量原子回刷**：Host 本地累加访问计数，跨机单次 8B 原子指令打包多个对象批量回刷 | 根除了控制面吞噬 86.6% 流量的弊端；消除了并发原子操作导致读吞吐暴跌 4.3 倍的物理冲突；吞吐提升 **1.4~3.1 倍** | 确立了分布式远端内存解耦池的黄金法则：严禁照搬单机细粒度 LRU，必须推行“粗粒度组级管理 + 本地计数”。 |
| **MEGALON**<br>*(OSDI '26)* | 领域三<br>(一致性树) | ① **分裂式元数据架构**：极简原子锁置于硬件一致性区，庞大索引树置于非一致性大容量内存<br>② **共享追加日志异步回放**：元数据变更通过顺序追加日志流式同步，各节点在本地构建只读索引副本 | 元数据换出开销**削减 8 倍**；打破了硬件一致性区仅数十 MB 的物理容量死穴；多机并发共享吞吐**提升 2.5 ~ 14.9 倍** | 厘清了硬件一致性的物理边界，提出了“核心极简一致 + 外部无锁日志回放”的大规模共享元数据最优范式。 |
| **Duhu**<br>*(OSDI '26)* | 领域三<br>(只读解耦) | ① **只读不可变数据与元数据本地化**：共享池中仅存放不可变只读 KV 数据，目录与生命周期在 Host 本地闭环<br>② **安全直通指针 (DuhuPtr)**：用户态直接解引用访问，消除 RPC 序列化与 Host 软中断开销 | 根除跨节点总线探测风暴，**消除 95% 以上的总线一致性税**；在无硬件一致性的内存硬件上达成线性扩展 | 树立了分布式大模型缓存设计的第一铁律：可变元数据跨机共享是万恶之源，唯有“不可变只读正文”才配进入共享池。 |
| **Beluga**<br>*(SIGMOD '26)* | 领域四<br>(CXL 直达) | ① **CXL 全局统一 DAX 地址空间**：通过内存映射将共享 CXL 内存映射为全局统一虚拟地址，CPU 直接解引用<br>② **发起者角色判定软件一致性**：按读写发起者角色在软件层下发轻量 Cache Flush/Invalidate，免除硬件总线广播 | 彻底打破传统网络协议栈繁重同步，小块访问从网络级 10µs+ 骤降至**纳秒/亚微秒级**；端到端 TTFT 取得数倍性能突破 | 标志着机架级共享从“网络消息中转时代”跨入“全局统一内存编址与直通语义时代”。 |
| **MemChannel**<br>*(NSDI '26)* | 领域四<br>(端侧流控) | ① **核心无状态端侧流控传输**：调度完全前置于 Host 端侧，规避不可编程交换机的硬件局限<br>② **CPU 性能计数器需求反推**：读取 Off-core 硬件计数器推导真实带宽需求，反向映射为线程时间配额 | 在完全不修改 CXL 交换机硬件的前提下，**消除大请求对小请求的队头阻塞**；高并发多租户争用时延降低数倍 | 填补了交换式共享内存网络缺乏传输层流控协议的空白，为机架级池化提供了低开销主机端侧调度规范。 |
| **Bidaw**<br>*(FAST '26)* | 领域五<br>(SSD 分层) | ① **计算—存储双向语义接口**：存储层向上暴露介质层级与能力，计算调度向下传递回答长度与重用距离<br>② **层级解耦双队列与 disk-HRRN**：分离就绪队列与准备队列，SSD 慢 I/O 采用响应比优先调度，消除队头阻塞 | 排队延迟**骤降 57.5%**；多层缓存未命中率**降低 57.6%~69.9%**；尾部延迟 P99 **下降 47.0%~56.8%** | 确立了“计算层语义是存储淘汰的黄金线索，存储介质层级是计算编排的前提条件”的双向感知协同准则。 |
| **SYMPHONY**<br>*(NSDI '26)* | 领域五<br>(预测预取) | ① **业务弱信号前置建议请求**：捕获用户打字或上游 Agent 调用前置信号，在请求到达前数秒提前触发异步预取<br>② **跨层流水重叠**：将多 GB 状态跨节点搬运完全从在线 TTFT 关键路径中剥离，转入后台静默预取 | 提示提前量平均达 **5.8~11.3 秒**；跨节点迁移开销彻底移出关键路径；节点负载不均衡度**降低 3.1 倍**；吞吐提升超 20% | 证明了通过引入 Host 侧微弱的业务先验信号，可以把昂贵的状态迁移从同步阻塞转化为异步静默流水。 |
| **KVServe**<br>*(SIGCOMM '26)* | 领域六<br>(动态选路) | ① **动态 Pareto 选路引擎**：离线构建“准确率—传输比—时延”最优集，在线依据链路实时带宽下包络做 $O(1)$ 选路<br>② **性能反噬前置避免**：当有效带宽过低时主动切回本地计算，确保拉取总开销严格小于直接重算耗时 | PD 分离下实现最高 **9.13 倍** 加速；长文本前缀缓存 TTFT **降低高达 32.8 倍**；剧烈抖动下时延毛刺由 0.9s **收敛至 0.3s** | 彻底打破了固化单一卸载策略的传统模式，将“传输与重算的权衡”转化为数学上可动态闭环的代数选路问题。 |
| **Finding NEMO**<br>*(OSDI '26)* | 领域六<br>(流式遥测) | ① **硬件流式遥测流水线**：在 Host 内存控制器/总线层嵌入硬件监听逻辑，实现 100% 事务覆盖且 **Host CPU 零开销**<br>② **亚微秒级能力矩阵输出**：向调度引擎实时提供租约状态、介质热度与持续带宽，杜绝信息滞后导致的错误决策 | 相比传统软件采样节省 **10%~20% CPU 算力**；彻底消除因滞后调度引发的 **23% 时延恶化与 1.7 倍吞吐暴跌** | 为上层微秒级调度引擎提供了第一手、无失真、零开销的硬件物理能力矩阵（CapabilityMatrix）。 |
| **RamRyder**<br>*(OSDI '26)* | 领域六<br>(物理隔离) | ① **物理内存通道硬隔离**：将低延迟前台在线业务与大吞吐后台搬运严格绑定至独立物理 Memory Channel<br>② **内存控制器硬件隔离**：消除跨任务在内存控制器调度队列与物理总线上的资源争用 | **消除 99% 的跨任务内存总线与控制器争用干扰**；使前后台混压状态下在线推理性能逼近物理独占硬件 | 指出仅靠软件优先级排队无法根治总线拥塞，在硬件物理通道层实现硬切分是保障极端尾部时延的终极底牌。 |
| **KVCodec**<br>*(SIGCOMM '26)* | 领域六<br>(独立卸载) | ① **旁路专用硬件卸载传输流水**：调用主机/加速卡上闲置的独立编解码专用单元处理数据压缩，零侵占主计算核<br>② **独立异步拉取队列**：在后台线程中静默执行解压与解包，动态根据网络带宽匹配码流，消除前台等待 | 在 1~40Gbps 低带宽网络下将非复用请求 TTFT **降低 77.1%**；完全避免了解压算力侵占在线推理 | 揭示了“利用芯片内部非核心专用硬件单元做数据路径旁路卸载”是打破通用算力争用的有效捷径。 |

---

## 第五部分 Host 侧技术选型坐标系与端到端决策链路

### 5.1 Host 侧技术选型五维正交坐标系

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
│                             KVCache Host 侧架构技术选型五维正交坐标系                            │
├──────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                  │
│   维度 1：存储与互联层级 (Storage & Interconnect Tier)                                           │
│   ├─ 本机 NUMA DDR 内存层 ───────────────────────────> DirectKV (Host bypass), Strata, ECHO     │
│   ├─ 集群网络与中继传输层 (Scale-Out Fabric) ─────────> DualPath, TurboBus, AITurbo              │
│   ├─ 远端解耦内存层 (Disaggregated Pool) ────────────> FORGE, MEGALON, Duhu                      │
│   ├─ CXL 交换式内存池层 (CXL Switched Memory) ───────> Beluga, MemChannel (CSFQ)                 │
│   ├─ NVMe SSD 分层沉淀层 (NVMe Tiering) ─────────────> Bidaw, SPDK/io_uring 用户态直读           │
│   └─ 全局动态决策与隔离层 (QoS & Scheduling) ─────────> KVServe, Finding NEMO, RamRyder          │
│                                                                                                  │
│   维度 2：数据通路语义 (Data Path Semantics)                                                     │
│   ├─ 内存直接编址直达 (DAX Direct Dereference) ───────> Beluga (CXL 2.0), Duhu (DuhuPtr)         │
│   ├─ 硬件 DMA / RDMA 零拷贝直通 (Zero-Copy DMA) ─────> Host CPU 零数据拷贝, TurboBus, DualPath  │
│   └─ 用户态异步 I/O (Asynchronous User-Space I/O) ────> SPDK, io_uring 4KB 对齐直接 I/O          │
│                                                                                                  │
│   维度 3：元数据与状态一致性 (Metadata & Consistency)                                            │
│   ├─ 只读不可变 + 元数据本地化 (Immutable Pool) ──────> Duhu, 本技术地图规范                    │
│   ├─ 分裂式元数据 + 共享追加日志 (Split & Log Replay) ─> MEGALON, FORGE                          │
│   └─ RCU 无锁并发目录管理 (RCU Tree Index) ──────────> 本技术地图规范                            │
│                                                                                                  │
│   维度 4：感知与前置预测 (Awareness & Forecasting)                                               │
│   ├─ 业务弱信号提前预测迁移 (Predictive Advisory) ────> SYMPHONY, Bidaw                          │
│   ├─ 硬件流式亚微秒级遥测 (Hardware Streaming Telemetry) -> Finding NEMO, 硬件物理能力矩阵       │
│   └─ 静态启发式 (Static Heuristics) ──────────────────> 传统 LRU/FIFO (生产中易反噬)             │
│                                                                                                  │
│   维度 5：服务质量与容错弹性 (QoS & Fault Tolerance)                                             │
│   ├─ 物理通道与硬件队列硬隔离 (Physical Hard QoS) ────> RamRyder, DualPath (VL), 硬件 QoS 隔离  │
│   └─ 视图租约安全守卫与回退 (Lease & Safe Rollback) ──> ViewGuard 机制                           │
│                                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 请求生命周期 Host 侧动态调度决策流

```
                             [推理请求到达 Host 侧调度入口]
                                           │
                                           ▼
                       [Step 1: 多维度语义一致性与租约前置检查]
                       (模型 / 词表 / 模板 / LoRA / 就绪位 / 租约)
                                           │
                 ┌─────────────────────────┴─────────────────────────┐
             [检查通过]                                          [校验未通过]
                 │                                                   │
                 ▼                                                   ▼
       [Step 2: 命中前缀与长度提取]                                 [回退至本地重算]
                 │
                 ▼
       [Step 3: 成本预估模型 (CostEvaluator)]
       (读取硬件物理能力矩阵：各级介质实时带宽、延迟与负载)
                 │
                 ▼
     [拉取与加载总开销 < 本地直接重算耗时 ?]
                 │
       ┌─────────┴─────────┐
    [是 (净收益为正)]   [否 (发生性能倒挂)] ─────────────────────────┐
       │                                                             │
       ▼                                                             │
[Step 4: 动态选路决策引擎 (QueryPlan)]                               │
       │                                                             │
 ┌─────┴─────────────────────────┬─────────────────────────┐         │
 ▼                               ▼                         ▼         │
[路径 A: CXL / 远端 DDR 直读]   [路径 B: 集群网络流式中继] [路径 C: NVMe SSD 加载]
(DAX 编址解引用直达)            (非对称多路径回源分发)     (SPDK 用户态 4KB 对齐)
 │                               │                         │         │
 ▼                               ▼                         ▼         │
[视图租约安全守卫机制]          [硬件 QoS 优先级队列隔离]  [disk-HRRN 双队列调度]
(捕获异常秒级平滑回滚)          (高优保证在线 TPOT 稳定)   (消除慢介质队头阻塞)
 │                               │                         │         │
 └───────────────────────────────┼─────────────────────────┘         │
                                 │                                   │
                                 ▼                                   ▼
                   [Step 5: 交付前台在线生成计算]               [本地直接 Prefill 重算]
                   (Host CPU 零数据拷贝直达完成)                     │
                                 │                                   │
                                 ▼                                   │
                   [生成 Token 流式返回用户] ◄───────────────────────┘
```

---

## 第六部分 总结与未来 AI Infra Host 侧演进前瞻

通过对 2026 年系统、存储与网络顶会最新研究成果的深度事实解构，结合上层 GPU/NPU 计算模式演进对下层基础设施的牵引，未来 2~3 年内 AI Infra 在 Host 侧的演进终局已然清晰：

1. **计算范式跃迁是 Host 侧架构演化的第一性牵引力**：
   从单体推理走向 PD 分离、Chunked Prefill、原生稀疏 MLA 与长程 Agent，上层计算与调度模式的每一次跃迁，都会以量级放大效应传递至 Host 侧。Host 基础设施唯有敏锐感知上层计算时序与状态意图，才能摆脱“信息黑盒与被动搬砖”的落后局面。

2. **Host CPU 彻底告别“搬砖工”，全面聚焦“调度脑”**：
   任何依赖 Host CPU 线程介入正文搬运的系统都将被大吞吐长上下文工作负载淘汰。Host CPU 零数据拷贝（Touch Bytes 严格为 0）不再是可选项，而是构建高性能大模型基础设施的绝对红线。CPU 将全部算力收敛至微秒级 QueryPlan 动态决策、RCU 元数据索引维护与硬件流控注入。

3. **CXL 与多介质统一直达全面取代传统网络消息存储栈**：
   对于细粒度、低延迟的 KVCache 共享，传统基于 Linux 内核与 TCP/RDMA 消息语义的存储系统开销过大。以 CXL 内存池、用户态 SPDK 与全局 DAX 编址为代表的“内存语义直达”架构正成为机架级互联的标配。

4. **前后台硬件级硬隔离是商业化落地的生命线**：
   在多租户与前后台混压的大规模生产环境中，任何纯软件排队调度都无法阻挡突发 I/O 对内存控制器和网络缓冲区的冲击。唯有建立在物理内存通道硬隔离与硬件 QoS 优先级队列之上的流控体系，才能牢牢守住 P99 尾部时延红线，确保业务 SLO 稳定达标。
